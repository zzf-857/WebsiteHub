# WebHub 产品需求文档（PRD）

> 文档版本：v0.9 Draft
> 更新日期：2026-07-28
> 当前状态：核心范围已确认；Q1-Q15 代码项已完成，Q14-Q15 页面能力待真实浏览器验收
> 需求依据：`00_Todolist/WebHub_Todolist/todolist.md`、账号与浏览器书签导入确认结论；不继承既有实施计划

## 1. 产品概述

### 1.1 产品定位

WebHub 是一个运行在现代浏览器中的 **Agent First 个人网站知识中枢**。它是一套具有账号、稳定 URL、页面导航和正常网页文档流的完整网站系统，不是原生桌面客户端或套在浏览器里的桌面软件界面。它不是在传统网址导航页上附加聊天框，而是让 Agent 成为网站收录、理解、检索与管理的主要入口，同时保留高效、可靠的可视化浏览和手动管理能力。

产品首先服务于家庭局域网中的多账号使用场景：所有设备访问同一个 WebHub 实例，用户通过账号登录自己的工作区。账号拥有独立的网站资料、组织结构、Agent 配置和偏好；首版不以公开 SaaS 或跨实例云同步为目标。

### 1.2 核心价值

1. 用户只需提交网址，Agent 即可提取核心信息并协助完成归档。
2. 用户可以用自然语言查询自己的收藏库，找不到时再使用真实联网搜索。
3. 用户可以用自然语言管理网站数据，但任何写操作都经过人审，避免误改和误删。
4. 分类、标签和 Space 共同解决“系统整理”和“按使用场景快速打开”的不同需求。
5. 用户始终能够看见答案来源，区分个人收藏与外部搜索结果。
6. 用户在局域网内任意设备登录后，都能进入与账号绑定的 WebHub 工作区。

## 2. 产品目标与非目标

### 2.1 第一版目标

- 做出可在家庭局域网长期使用的完整 MVP，而非仅用于演示的原型。
- 跑通“提交网址 -> AI 分析 -> 人工确认 -> 入库 -> 检索 -> 再利用”的完整闭环。
- 让 Agent 对话成为首要交互，同时保证关键词搜索和手动管理足够快捷。
- 支持唯一分类、多标签、多 Space 的网站组织模型。
- 内置 Tavily、Jina、Exa 三种真实联网搜索 Provider，由用户自行配置 API Key。
- 支持局域网内的简单注册、登录和账号级数据隔离，配置随账号加载。
- 支持 Chrome、Edge、Firefox、Safari 等常见浏览器导出的 Netscape Bookmark HTML，在大数据量下完成可恢复解析、分类预览和确认入库。
- 在不明显增加无效复杂度的前提下，让 Vercel AI SDK、LangChain/LangGraph、LlamaIndex 分别承担真实职责。

### 2.2 第一版非目标

- 公网开放、公开导航站、SEO 或商业化运营。
- 邮箱/短信验证、第三方 OAuth、复杂角色权限、家庭协作空间和云端同步。
- 移动端原生 App，以及 MVP 阶段的完整手机/平板响应式适配；移动 Web 适配进入增强版本。
- 自动生成网站预览截图、自动关联 GitHub 仓库或其他关联站点；这些进入后续增强版本。
- 网站可用性定时巡检、失效链接监控和自动清理。
- 让 LLM 在没有个人收藏或联网搜索依据时编造网址。

## 3. 使用者与运行环境

### 3.1 目标使用者

- 主要使用者：需要长期收集开发工具、学习资料、AI 工具和生活服务网站的个人用户。
- 次要使用者：同一家庭局域网内注册独立账号的家庭成员。
- 每个账号拥有独立工作区；默认不能浏览、检索或修改其他账号的数据与配置。

### 3.2 典型设备

- MVP 访问端：Windows、macOS、Linux 的现代桌面浏览器，优先完成 Chrome / Edge 体验。
- 桌面兼容范围：在不依赖特定浏览器扩展的前提下兼容当前主流 Firefox 和 Safari。
- 增强版本：手机和平板浏览器的分辨率、触控交互和响应式布局适配。
- MVP 由当前 Windows 电脑运行 WebHub 服务，局域网内其他设备只需要浏览器即可访问。
- Docker、NAS 或 Linux 常驻服务器部署进入后续增强版本。

## 4. 核心领域模型

| 概念 | 定义 | 约束 |
|---|---|---|
| 账号（Account） | 用户身份及其工作区的数据所有者 | 用户名在当前 WebHub 实例内唯一 |
| 网站（Site） | WebHub 管理的基本对象 | 规范化 URL 在同一账号内唯一 |
| 分类（Category） | 网站所属的大类，如“技术”“学习”“日常” | 每个网站只能有一个分类；无合适分类时进入“未分类” |
| 标签（Tag） | 描述网站能力、主题或用途的细粒度关键词 | 每个网站可有多个标签；需去重和规范化 |
| Space | 用户按项目或使用场景创建的网站集合 | 网站可同时加入多个 Space；Space 可自定义成员顺序 |
| 置顶（Pinned） | 标记高频使用的网站 | 与分类、标签、Space 相互独立 |
| 会话（Conversation） | 当前账号与 Agent 的一组连续消息 | 归单一账号所有；不同会话上下文互不混用 |
| 书签导入任务（Bookmark Import Job） | 一次浏览器书签文件的解析、分类、预览与提交过程 | 归单一账号所有；可恢复、可取消并记录来源文件摘要与阶段状态 |
| 源目录（Source Folder） | 浏览器导出文件中的原始文件夹节点 | 保留父子关系、原始名称、同级顺序和空目录，不等同于 WebHub 分类 |
| 书签出现项（Bookmark Occurrence） | 某个书签在一次源目录树中的具体出现位置 | 保留原始 URL、标题、目录路径、顺序和可用的源属性；多个出现项可关联同一个 Site |

### 4.1 网站核心字段

MVP 至少维护以下信息：

- 名称
- 原始 URL 与规范化 URL
- 一句话描述
- favicon
- 唯一分类
- 多个标签
- 所属 Space 列表
- 是否置顶
- 创建时间与更新时间
- 信息来源和最近一次分析状态

### 4.2 URL 身份与书签来源

- 同一账号内的 Site 以保守规范化后的 `identity URL` 判定唯一性。规范化只能处理协议/主机大小写、默认端口和等价路径等不会改变资源语义的部分，必须原样保留 query 与 fragment，不得删除、排序、解码或重写其中参数。
- query 或 fragment 不同的 URL 视为不同 Site；完全相同的 identity URL 可合并到同一个 Site，但不得静默覆盖已有名称、描述、分类、标签、置顶或 Space 关系。
- 浏览器导入中的每个书签位置都保存为独立 occurrence。即使同一 URL 同时存在于多个目录、使用不同标题或在不同导入快照中出现，也不得丢失原目录路径、标题和顺序。
- 浏览器 HTML 内嵌的 `ICON`、`ICON_URI` 等 favicon 数据一律丢弃，不进入暂存区、数据库、日志或模型上下文；Site 后续需要图标时按正常安全抓取流程重新获取。

### 4.3 分类与标签治理

- Agent 默认优先从已有分类中选择；确实不适合时，可在预览中提出一个新分类。
- 新分类必须经过用户确认后创建，避免分类无限膨胀。
- Agent 为单个网站建议 2 至 8 个高信息量标签，不以标签数量多为目标。
- 标签保存前进行首尾空格、大小写和完全重复项归一化；同义标签合并作为后续增强能力。
- 浏览器书签批量分类先使用源目录树、目录簇、域名与已有分类映射；只有低置信度目录簇才可进入受预算约束的 LLM 批量分类，禁止按每条书签无上限调用模型。

## 5. 核心用户流程

### 5.1 注册与登录

1. 用户首次访问 WebHub 时进入登录页，可使用用户名和密码创建账号。
2. 注册成功后自动进入该账号的独立工作区，不要求邮箱、手机号或第三方账号。
3. 后续访问可使用同一账号登录；有效会话可在受信任设备上保持，用户也可主动退出。
4. 登录后加载该账号的网站、分类、标签、Space、主题偏好、LLM 配置和搜索 Provider 配置。
5. 所有查询、索引、Agent 动作和数据导入导出都必须限定在当前账号范围内。
6. 忘记密码首版由 WebHub 服务拥有者在本机执行重置，不建设邮件找回流程。

### 5.2 对话与历史记录

1. 用户发送首条消息时创建一个新会话，后续消息只使用当前会话作为对话上下文。
2. 消息、来源信息和可重放的结构化结果按账号保存到服务端，在其他设备登录后仍可查看。
3. 历史会话按照最后活跃时间自动分为“今天”“昨天”“近 7 天”“近 30 天”和更早月份。
4. 会话标题默认取首条用户消息的精简文本，不额外消耗一次 LLM 调用；用户可以重命名或删除会话。
5. 输入框首个非空字符为 `/` 时打开命令建议面板，支持键盘选择和参数提示。
6. MVP 提供 `/搜索 <关键词>` 与 `/存入 <一个或多个 URL>`；命令最终仍进入与自然语言相同的检索、预览和确认流程。
7. 命令系统采用可注册的命令定义，为后续新增命令保留稳定扩展入口；未知命令只提示，不发送给 LLM 或执行动作。

### 5.3 单个或批量收录网站

1. 用户在 Agent 对话框、快速收录入口或批量导入界面提交一个或多个 URL。
2. 系统校验并规范化 URL，识别重复项和无效项。
3. 系统抓取页面标题、描述、favicon 和可用于分析的正文信息。
4. 抓取失败时，可调用已配置的正文读取服务辅助提取；仍失败则保留 URL 并允许手工补全。
5. Agent 生成名称、描述、分类和标签建议。
6. 前端展示逐条可编辑预览，明确标出成功、重复、抓取失败或仅凭有限信息推断等状态。
7. 用户勾选并确认后才正式写入资料库；单条失败不得阻塞其他条目。

### 5.4 导入大规模浏览器书签

1. 用户在当前账号的导入页选择 Chrome、Edge、Firefox 或 Safari 导出的 Netscape Bookmark HTML。上传只创建账号隔离的暂存任务，不直接写入 Site、分类或标签。
2. 服务端以流式/增量方式解析文件，保留完整源目录树、同级顺序和每个书签 occurrence；浏览器导出的内嵌 favicon 在解析入口即丢弃。
3. 系统对每个条目进行格式与协议判断，分别统计可导入、账号内已有、文件内重复、私网/本机地址、敏感 URL、无效与不支持项。非 HTTP(S) 等不支持项保留在报告中并说明原因，不得静默跳过。
4. 第一阶段展示“解析预检”：文件摘要、目录与 occurrence 数量、唯一 URL 数量、重复合并规则、不支持/私网/敏感项、预计分类方式和 LLM 调用上限。此时业务资料库无变化。
5. 用户可取消任务，或确认进入分析阶段；可选择导入范围、已有站点处理方式、目录到分类的映射和允许的 LLM 预算。
6. 系统优先按源目录簇、已有映射、域名和规则生成分类/标签候选。同一目录簇复用结果；只有低置信度簇才批量调用当前账号配置的 LLM，不按单条 URL 逐一调用。
7. 未配置模型、预算为零/耗尽、Provider 失败或任务被取消时，系统降级为确定性分类；无法可靠归类的条目进入“未分类”，不得阻塞导入。
8. 含凭据、token、session、签名参数等敏感特征的 URL 不发送给 LLM。HTTP(S) 私网、本机及保留地址允许收藏，但服务端不得抓取或跟随重定向访问，只能根据本地解析信息归档并明确标记“仅保存”。
9. 第二阶段展示“入库变更预览”：将新建/合并的 Site、保留的 occurrences、分类与标签变更、未分类项、仅保存项和不支持项分组分页展示，并允许按目录簇批量编辑。
10. 用户第二次明确确认后，系统才按可恢复的小批事务写入当前账号。已有 Site 只增加来源/occurrence 或用户明确勾选的字段，不得被导入数据静默覆盖。
11. 任务持续展示聚合进度、当前阶段、已处理/总数、告警和预算消耗；支持取消、服务重启后恢复以及失败分片重试，不要求浏览器页面持续打开。
12. 相同任务的重试、重复确认和断点恢复必须幂等。重新上传相同文件摘要时先提示已存在的导入快照，由用户选择查看、继续或显式创建新快照，不能默默制造重复 occurrence。

### 5.5 查询网站

1. 用户用自然语言描述需求，例如“找一下 Unity API 文档”。
2. 系统先检索个人收藏库，并把命中结果标注为“收藏库”。
3. 若收藏库结果不足，且当前允许联网，系统调用用户选中的外部搜索 Provider。
4. 外部结果标注具体 Provider 和原始网站来源，并提供“一键收录”。
5. 一键收录仍进入可编辑预览，不得绕过确认流程。

### 5.6 用自然语言管理资料库

1. 用户提出修改要求，如“把 GitHub 改到开发分类并置顶”。
2. Agent 将自然语言转换为明确的候选变更。
3. 前端以结构化差异预览展示“改前 / 改后”。
4. 用户确认后执行；取消则资料库不发生变化。
5. 删除和批量变更在差异预览之后还需明确二次确认。

### 5.7 使用 Space

1. 用户创建 Space，例如“开发一个 Unity 游戏”或“出国旅行”。
2. 用户可手动或通过 Agent 预览确认，将任意已收录网站加入一个或多个 Space。
3. Space 内支持拖拽或其他明确方式调整顺序。
4. 用户可一键打开 Space 中全部网站，并选择“新标签页”或“新窗口”模式。
5. 浏览器可能合并新窗口、转为标签页或拦截批量弹窗；系统必须在操作前给出清晰提示，并对未打开项提供重试入口。

## 6. 功能需求

### 6.1 账号与会话

| 编号 | 需求 |
|---|---|
| FR-AU-001 | 局域网内用户可使用唯一用户名和密码自助注册，不依赖邮箱、短信或第三方 OAuth。 |
| FR-AU-002 | 支持登录、保持会话、主动退出和修改密码。 |
| FR-AU-003 | 网站、分类、标签、Space、RAG 索引、对话数据和导入导出均按账号隔离。 |
| FR-AU-004 | LLM、Embedding、搜索 Provider、主题和其他用户偏好跟随账号。 |
| FR-AU-005 | 除注册、登录和必要的服务健康检查外，应用数据接口必须要求有效登录态。 |
| FR-AU-006 | 用户不能通过猜测资源 ID、修改请求参数或 Agent 指令访问其他账号资源。 |
| FR-AU-007 | 首版提供服务端本机密码重置方式；不建设邮件找回密码流程。 |

### 6.2 Agent 首页与对话

| 编号 | 需求 |
|---|---|
| FR-AI-001 | 首页首屏以 Agent 输入区为视觉和交互中心，明显高于普通导航浏览区的优先级。 |
| FR-AI-002 | 对话响应支持流式展示，长耗时任务需持续显示当前阶段和逐条进度。 |
| FR-AI-003 | Agent 可执行站内查询、联网查询、收录预览、修改预览、删除候选选择和 Space 管理预览。 |
| FR-AI-004 | Agent 的站点结论必须附来源；外部网址只能来自用户输入、资料库或真实搜索 Provider。 |
| FR-AI-005 | LLM 或外部服务不可用时，站内关键词搜索和手动管理仍可正常使用。 |
| FR-AI-006 | 支持创建、切换、重命名和删除多个会话；每个会话的上下文彼此独立。 |
| FR-AI-007 | 会话与消息按账号保存在服务端，并在同一账号的不同设备间同步。 |
| FR-AI-008 | 历史会话按最后活跃时间分为今天、昨天、近 7 天、近 30 天和更早月份。 |
| FR-AI-009 | 会话标题默认由首条用户消息生成，用户可手动重命名；历史记录在用户删除前不自动过期。 |
| FR-AI-010 | 持久化内容需保留文本、来源引用和结构化结果；失效的网站或动作应显示过期状态，不能从历史记录直接执行陈旧确认。 |
| FR-AI-011 | 输入框支持以 `/` 唤起的命令面板，MVP 内置 `/搜索` 和 `/存入`。 |
| FR-AI-012 | Slash Command 通过统一注册接口扩展；命令不得绕过账号鉴权、参数校验、预览或二次确认。 |
| FR-AI-013 | Assistant 正文按流式 Markdown/GFM 渲染，列表、表格、强调和代码不得向用户暴露原始标记；工具结果与网站信息使用结构化卡片，未知旧版载荷显示兼容提示而不是原始 JSON。 |
| FR-AI-014 | Provider 明确返回的 reasoning 以可折叠区域流式展示：生成时自动展开、结束后自动收起，并显示服务端实测思考耗时；Provider 未返回 reasoning 时不得从普通正文推断或伪造。 |
| FR-AI-015 | 回答耗时、首 Token 延迟和 Token 用量作为消息元数据保存并可跨设备恢复；Token 仅展示 Provider 明确返回的 usage，缺失字段隐藏，不做本地估算。 |
| FR-AI-016 | Assistant 正文中的 HTTP(S) 链接渲染为安全的紧凑跳转卡；收藏库工具结果优先进入站内详情，只有外部搜索结果可直接打开外站。 |

### 6.3 网站收录

| 编号 | 需求 |
|---|---|
| FR-IN-001 | 支持单 URL 和多 URL 批量提交，批量输入至少支持每行一个 URL。 |
| FR-IN-002 | URL 规范化后去重；重复项不得静默覆盖原记录。 |
| FR-IN-003 | 自动提取名称、描述和真实 favicon，并生成分类、标签建议；依次尝试页面声明的常见图标链接，声明缺失或失败时独立尝试站点根路径 `/favicon.ico`、`/favicon.png`、`/favicon.svg`、`/apple-touch-icon.png`，即使 HTML 因 403、超时或反爬无法读取也不跳过根路径图标。每个候选及重定向逐跳执行 SSRF、DNS 固定、大小、MIME 与图片签名校验；通用 MIME 只有通过图片 magic 才可接受，SVG 保持严格校验。所有候选均不可用时显示本地默认图标，禁止调用任何第三方 favicon CDN。 |
| FR-IN-004 | 所有新增内容必须经过可编辑预览和用户确认。 |
| FR-IN-005 | 批量任务按条目返回状态，支持部分成功、取消未开始任务和重试失败项。 |
| FR-IN-006 | 抓取失败时允许用户直接手工补全，不因 AI 不可用而无法收藏。 |

### 6.4 检索与推荐

| 编号 | 需求 |
|---|---|
| FR-SE-001 | 提供独立站内关键词搜索，适合已知网站名称或明确关键词的快速查找。 |
| FR-SE-002 | Agent 默认先检索当前账号的收藏库；结果不足时自动调用该账号启用的联网 Provider，除非用户选择“仅收藏库”。 |
| FR-SE-003 | 站内检索同时利用名称、URL、分类、标签和描述；语义检索不能取代精确关键词命中。 |
| FR-SE-004 | 外部搜索结果统一展示名称、URL、摘要、来源 Provider 和可用的 favicon。 |
| FR-SE-005 | 用户可以切换“仅收藏库”和“允许联网”范围。 |
| FR-SE-006 | 无可靠结果时明确告知，不得用模型记忆伪造可点击网址。 |

### 6.5 可视化浏览与手动管理

| 编号 | 需求 |
|---|---|
| FR-LI-001 | 支持按分类、标签、Space 和置顶状态浏览或筛选网站。 |
| FR-LI-002 | 支持按名称、添加时间、更新时间和用户自定义顺序排序。 |
| FR-LI-003 | 支持手动新增、编辑、删除网站，不依赖 Agent。 |
| FR-LI-004 | 已收录的网站卡片至少展示 favicon、名称和简短描述；点击后进入站内详情页，仅通过明确的“访问官网”按钮在新标签页打开外站。外部搜索结果仍可直接跳转外站。 |
| FR-LI-005 | 网站详情视图展示完整核心字段，并可进入编辑状态；页面声明了经安全校验的 `og:image` / `twitter:image` 时，以紧凑、稳定比例的预览展示并支持点击放大、关闭按钮、背景点击和 Escape 收起；图片缺失或加载失败时不保留空占位。 |
| FR-LI-006 | 置顶网站进入独立的高频访问区域，但不改变原分类和 Space 关系。 |
| FR-LI-007 | 资料库批量模式同时提供“全选”（当前筛选命中的全部结果）和“全选（已加载）”（浏览器当前实际加载的全部网站），选择数量不受删除批次上限截断。“全选”必须由服务端返回账号内、带版本号的轻量筛选快照，不得靠文案假装覆盖未加载结果。删除前只确认一次，随后按单批最多 100 条串行提交；每批按账号和乐观锁版本保证全有或全无，冲突时停止后续批次、刷新并要求重新选择。 |
| FR-LI-008 | 置顶与普通网站列表各自使用游标静默自动加载下一页，在接近列表末尾时提前触发；现代浏览器主路径不得依赖“加载更多”按钮，不得重复请求同一游标，并需丢弃筛选切换后的陈旧响应。运行环境不支持 `IntersectionObserver` 时必须保留可操作的分页降级入口，不能让后续数据永久不可达。 |

### 6.6 分类、标签与 Space

| 编号 | 需求 |
|---|---|
| FR-OR-001 | 支持分类的创建、重命名和删除；删除分类前必须预览受影响网站。 |
| FR-OR-002 | 分类名在同一账号内唯一；每个网站恰好属于一个分类。 |
| FR-OR-003 | 支持标签的添加、移除、重命名和合并；网站可拥有多个标签。 |
| FR-OR-004 | 支持 Space 的创建、重命名、删除、成员增删和成员排序。 |
| FR-OR-005 | 同一网站可加入多个 Space；删除 Space 不得删除其中的网站。 |
| FR-OR-006 | Space 支持一键批量打开，并正确处理浏览器弹窗拦截或部分失败。 |

### 6.7 配置中心

| 编号 | 需求 |
|---|---|
| FR-CF-001 | LLM 配置内置 OpenAI、DeepSeek、通义千问、Kimi、Ollama，并提供 OpenAI-compatible 自定义入口。 |
| FR-CF-002 | 每个账号可保存多个 Provider 配置，并分别设置 Base URL、API Key 和模型名；调用时只使用当前启用项。 |
| FR-CF-003 | 搜索 Provider 内置 Tavily、Jina、Exa，每个账号独立选择一个当前启用项。 |
| FR-CF-004 | 每个搜索 Provider 提供 API Key 申请入口、Key 输入、连接测试和能力说明。 |
| FR-CF-005 | API Key 按账号由服务端加密保存，前端保存后只显示掩码，不回传完整值。 |
| FR-CF-006 | MVP 不做自动 Provider 故障转移，避免用户不知情的额外 API 消耗。 |
| FR-CF-007 | MVP 由用户手动填写模型名；自动拉取模型列表和模型能力检测进入增强版本。 |
| FR-CF-008 | 每个模型和搜索 Provider 都显示可辨识的品牌图标、名称、连接状态和启用状态；自定义 Provider 使用统一通用图标。 |
| FR-CF-009 | Ollama 支持配置局域网或本机 Base URL，API Key 可留空；其他预设按各厂商要求校验必要字段。 |
| FR-CF-010 | Provider 配置支持独立测试连接；测试失败不得覆盖上一次可用配置或自动切换当前 Provider。 |
| FR-CF-011 | Provider 页面只承载模型、搜索和向量 Provider 配置；语义索引状态与回填操作使用独立设置页，不得混入 Provider 页面或显示书签导入状态。 |

搜索服务申请入口：

- Tavily：[API 控制台](https://app.tavily.com/home)
- Jina：[API Key 控制台](https://jina.ai/api-dashboard)
- Exa：[API Key 控制台](https://dashboard.exa.ai/api-keys)

### 6.8 数据导入导出

| 编号 | 需求 |
|---|---|
| FR-DA-001 | 支持导出当前账号的网站、分类、标签、Space 和会话数据，保证个人资料可迁移。 |
| FR-DA-002 | 支持向当前账号导入 WebHub 自有备份格式；执行前校验并展示变更摘要。 |
| FR-DA-003 | MVP 支持 Chrome、Edge、Firefox、Safari 常见导出形式的 Netscape Bookmark HTML；解析器需兼容标签/属性大小写、常见字符集声明和浏览器扩展属性，不依赖浏览器扩展。 |
| FR-DA-004 | 上传文件只创建当前账号可见的暂存导入任务；解析、预览、任务查询、取消、确认和结果中的每个对象都必须由服务端强制限定 `user_id`。 |
| FR-DA-005 | 导入必须保留源目录树的父子关系、名称和顺序，并为文件中的每个书签位置保存 occurrence；同一 URL 在多个目录或以不同标题出现时不得折叠掉来源信息。 |
| FR-DA-006 | URL identity 采用保守规范化，必须逐字保留 query 与 fragment 的内容和顺序；query/fragment 不同的 URL 不得合并。原始 URL 同时保存在 occurrence 中。 |
| FR-DA-007 | 相同 identity URL 在同一账号内关联一个 Site 和多个 occurrences；命中已有 Site 时默认保留已有业务字段，只追加导入来源，不静默覆盖名称、描述、分类、标签、置顶或 Space。 |
| FR-DA-008 | 解析入口必须丢弃书签 HTML 内嵌的 favicon 数据；无效 URL、非 HTTP(S) 协议、浏览器内部页及其他不支持节点需按原因进入预览统计和明细，不得静默丢弃或误入库。 |
| FR-DA-009 | HTTP(S) 私网、本机、link-local、保留地址和云元数据目标允许作为“仅保存”书签入库，但服务端不得抓取、解析重定向或调用外部读取服务访问这些目标。 |
| FR-DA-010 | 导入采用两阶段确认：先展示解析预检并确认分析范围/预算，再展示最终入库变更并确认写入；第二次确认前不得改变 Site、Category、Tag、Space 或 occurrence 业务数据。 |
| FR-DA-011 | 第一阶段预检至少展示目录数、occurrence 数、唯一 URL 数、文件内重复、账号内已有、私网/敏感、无效/不支持项、预计规则分类覆盖率和 LLM 调用上限。 |
| FR-DA-012 | 分类优先使用源目录簇、用户映射、已有分类、域名和确定性规则；仅对低置信度目录簇进行去重后的 LLM 批量分类，不得默认逐 URL 调用模型。 |
| FR-DA-013 | 用户可在分析前看到并限制最大 LLM 簇数、token/费用或等价调用预算；未配置 Provider、预算耗尽、超时或失败时自动降级，剩余条目进入规则结果或“未分类”，已完成结果不回滚。 |
| FR-DA-014 | 检测到凭据、token、session、签名或其他敏感参数特征的 URL 时，不得把完整 URL、query、fragment 或相关秘密属性发送给 LLM；该条目只走本地规则并在预览中标记。 |
| FR-DA-015 | 第二阶段预览至少按新建 Site、合并已有 Site、保留 occurrence、分类/标签候选、未分类、仅保存和不支持项聚合，并支持分页、搜索、按目录簇批量修改及抽样检查。 |
| FR-DA-016 | 大规模导入必须使用持久化异步任务，支持阶段检查点、取消未开始分片、服务重启后恢复和失败分片重试；浏览器断开不能导致任务状态丢失。 |
| FR-DA-017 | 同一导入任务的重试、断点恢复、重复提交和重复确认必须幂等；相同文件摘要的再次上传需提示已有快照，只有用户显式选择时才创建新的导入快照。 |
| FR-DA-018 | 大规模预览只返回聚合统计和分页明细，不向浏览器或 Agent 上下文一次发送全量条目；任务应持续提供阶段、计数、告警、降级状态和预算消耗。 |
| FR-DA-019 | WebHub 内置版本化 `import-browser-bookmarks` Skill，固化格式识别、URL identity、目录/occurrence、分类、预算、安全、两阶段确认和幂等规则；任何内部 Agent 必须通过同一后端导入工具调用，不得自行改写解析规则、直写数据库或绕过确认。 |
| FR-DA-020 | 每次导入保留账号可见的来源摘要、规则/Skill 版本、参数、预算、阶段统计、确认人/时间和最终结果，以便复查与可重复处理；不保留被丢弃的 favicon 或明文敏感模型载荷。 |

## 7. Agent 行为与安全边界

### 7.1 操作分级

| 操作类型 | 示例 | 执行规则 |
|---|---|---|
| 只读 | 查询、筛选、总结、联网搜索 | 可直接执行并返回来源 |
| 新增 | 收录网站、创建分类、创建 Space | 必须展示可编辑预览，确认后写入 |
| 修改 | 改名、改分类、改标签、置顶、调整 Space | 必须展示变更差异，确认后写入 |
| 删除 | 删除网站、分类、标签或 Space | 先展示影响范围，再进行明确二次确认 |
| 批量操作 | 批量改分类、批量删除、批量加入 Space | 展示逐项清单和数量，再进行明确二次确认 |
| 大规模书签导入 | 解析浏览器 HTML、自动分类并批量入库 | 解析预检确认与最终变更确认分离；只在第二次确认后分批幂等写入 |

### 7.2 约束

- LLM 只能提出结构化候选动作，不能直接访问数据库。
- 服务端只接受白名单动作和经用户确认生成的短期操作凭证；凭证必须绑定当前账号和会话。
- 历史消息可以重放展示，但其中过期或已经执行的操作凭证必须失效。
- 预览后数据发生变化时，原确认失效，必须重新生成预览。
- 每次成功写入后返回清晰的变更结果；失败时不得显示为成功。
- 外部页面内容视为不可信数据，不得把网页中的指令当成系统指令执行。
- 内置 Skill 是 Agent 的操作合同，不是授权边界。Agent 只能调用账号限定的导入 API，解析、隐私过滤、预算和确认规则必须由服务端代码再次强制执行。
- 浏览器导出中的标题、目录名、URL 和属性均视为不可信输入；展示时需转义，不得执行其中的 HTML、脚本或命令文本。

## 8. 联网搜索 Provider 规范

三家 Provider 通过统一适配层接入，对上层至少提供：

- 标题
- URL
- 摘要或相关片段
- favicon（Provider 提供时）
- 发布时间（Provider 提供时）
- Provider 名称
- 原始相关性分数（Provider 提供时，仅供内部排序）

能力侧重点：

- **Tavily**：通用实时搜索、相关片段和页面提取能力，适合作为常规查询 Provider。
- **Jina**：`s.jina.ai` 用于搜索，`r.jina.ai` 用于把网页转换为适合 LLM 的正文，适合辅助收录分析。
- **Exa**：搜索与内容提取字段丰富，适合语义发现、摘要和更深的资料检索。

每个账号选择自己的当前 Provider；切换只影响该账号的后续请求，不改变已经入库的网站。Provider 失败时应明确提示，可由用户手动切换，不在 MVP 中自动扣费重试其他 Provider。

官方能力参考：

- [Tavily Search API](https://docs.tavily.com/documentation/api-reference/endpoint/search)
- [Jina Reader / Search](https://jina.ai/reader)
- [Exa Search API](https://exa.ai/docs/reference/search)

## 9. 体验与视觉要求

- 首页是实际可用的 Agent 工作台，不制作营销落地页。
- 登录后的核心页面使用统一的网站 Header，提供 WebHub 品牌、Agent、资料库、Space、主题和账号入口；设置通过账号菜单或独立入口访问。
- Agent、资料库、Space 和设置使用稳定、可刷新、可深链的独立 URL；浏览器前进、后退和地址栏直接访问必须符合预期。
- 页面采用有最大宽度和响应式页边距的正常网页内容容器，允许内容沿主文档流向下延伸，不把全站固定在 `100vh` 应用 Shell 中。
- 会话历史只属于 Agent 页面，可作为页面内的可折叠次级区域、网页抽屉或独立历史页；不得成为贯穿资料库、Space 和设置的常驻全高侧栏。
- 移动端增强版本将站点导航和 Agent 页内历史适配为响应式菜单或抽屉；切换页面或会话后自动关闭临时抽屉。
- Agent 区占据首屏主要空间；分类浏览、置顶、最近收录和 Space 位于同一产品界面的后续区域或导航视图。
- Slash Command 面板显示命令名称、用途和参数提示，并随输入过滤；用户未选择命令前不得自动执行。
- 大规模书签导入使用独立、可刷新和可深链的任务页面；默认展示聚合摘要、目录簇和分页明细，不渲染十万条卡片，也不要求聊天上下文承载全量条目。
- Provider 设置采用紧凑、清晰的品牌选择器；品牌图标尺寸和视觉权重保持一致，不让高饱和品牌色破坏整体克制风格。
- 厂商品牌资源随应用本地打包，不从第三方 CDN 热加载；优先使用厂商官方资源或有明确授权来源的图标，并适配浅色、深色和高对比度环境。
- 视觉参考 Apple 的克制、留白、层级和动效节奏，避免过多色彩和装饰性卡片。
- 提供浅色、深色和跟随系统三种主题；首次默认跟随系统，记住用户手动选择。
- ReactBits 仅用于有明确交互价值的动效，不能影响信息密度、可访问性或低性能设备使用。
- 支持键盘操作、清晰焦点态、足够对比度和 `prefers-reduced-motion`。
- MVP 优先保证桌面端完整体验；手机和平板响应式适配不作为首版验收阻塞项。
- 1440x960 等效果图尺寸表示浏览器内容视口，不表示网页固定高度；首屏应保留向下滚动和后续内容的自然延续。

## 10. 非功能需求

### 10.1 性能与容量

- 目标容量：单账号至少可持有 100,000 个 bookmark occurrences；当它们全部是不同 identity URL 时，资料库仍可接受并管理 100,000 个 Site。
- 本地关键词查询目标响应时间：100,000 个 Site 数据下 P95 小于 1 秒；列表和筛选必须服务端分页，不能把全量 Site 下发到浏览器。
- 普通列表筛选和排序应在用户感知上即时完成，不因 Agent 状态阻塞。
- 批量收录至少支持一次提交 50 个 URL，并展示持续进度。
- 浏览器书签导入是独立的大规模任务，不受“最多 50 个 URL”限制；MVP 性能门槛为单文件 100,000 个 occurrences。
- 在记录硬件与数据集版本的 Windows 参考开发机上，100,000 occurrence 合成基准的“解析 + 暂存 + identity 去重 + 第一阶段预检”应在 120 秒内完成，parser/preview 的额外 Python 分配峰值不超过 128 MiB，导入 worker 峰值工作集不超过 512 MiB；最终纯本地事务提交应在 5 分钟内完成。联网抓取与 LLM 等外部耗时不计入该门槛。
- 100,000 occurrence 任务的聚合状态或任一分页预览（每页不超过 200 条）P95 小于 1 秒；取消请求在 5 秒内进入“正在取消/已取消”，并停止调度新分片。
- LLM、网页抓取和外部搜索均需独立超时、取消和部分失败处理。

### 10.2 可靠性

- 单个 URL 抓取或分析失败不得导致整批失败。
- 所有写操作需要事务性：要么按预览结果完整提交，要么返回明确的逐项失败结果。
- 数据变更后，站内检索索引应在可接受时间内更新；索引失败不能造成主数据丢失。
- 用户数据和配置应支持备份与恢复。
- 导入任务以稳定检查点记录解析、分类、预览和提交进度；进程重启后只重做未完成分片，已提交分片不得重复创建 Site 或 occurrence。
- 解析/分类失败不能污染主资料库；暂存数据达到保留期后可清理，但最终导入报告和 occurrence 来源关系按账号保留。

### 10.3 安全与隐私

- 服务默认只面向受信任的家庭局域网，不承诺公网暴露安全性。
- 密码只保存强哈希，不得以明文或可逆形式保存；会话使用服务端可撤销的安全 Cookie。
- API Key 和模型凭据按账号加密保存，不得进入浏览器日志、聊天记录或导出的普通数据文件。
- 所有数据库查询和 RAG 检索必须显式带账号范围，不能只依赖前端隐藏数据。
- 网页抓取需防止访问本机、局域网保留地址和云元数据地址，降低 SSRF 风险。
- 外部网页正文在进入 LLM 前应标记为不可信内容，并限制长度。
- 书签文件大小、节点深度、单字段长度和总 occurrence 数必须设置上限；解析过程不得执行 HTML/JavaScript、加载外部资源或信任内嵌 favicon。
- 私网/本机 HTTP(S) 书签可保存但不可由服务端抓取；非 HTTP(S) 和浏览器内部协议默认不入库，并必须在预览中可审计。
- 敏感 URL 检测和模型载荷过滤在服务端执行并有测试证明；完整 query/fragment 不进入模型调用、普通日志或错误追踪。

### 10.4 平台与浏览器兼容

- 使用标准 Web 能力实现，不把 Chrome 扩展或特定桌面系统 API 作为核心功能依赖。
- MVP 以桌面宽度和键鼠交互为基准，支持 Windows、macOS、Linux 浏览器访问。
- 主要验收 Chrome / Edge，并通过 Firefox 与 WebKit 兼容性检查关键流程。
- 移动端适配在增强版本完成，包括导航抽屉、触控目标、窄屏表单、聊天输入区和 Space 操作。

### 10.5 首版部署边界

- MVP 优先提供适合当前 Windows 开发机的本地启动与局域网访问方式。
- 服务端监听地址和端口必须可配置，并在启动时清楚显示局域网访问地址。
- 首版不要求用户安装 Docker；后续再提供 Docker Compose 与 NAS/Linux 部署说明。
- 部署方式变化不能改变账号的数据所有权和导入导出格式。

## 11. MVP 验收场景

1. 两个局域网用户分别注册并登录，各自只能看到自己的工作区、索引、配置和聊天记录。
2. 用户退出后不能继续访问受保护接口；重新登录后恢复该账号的数据和偏好。
3. 用户在设备 A 创建多个会话，设备 B 登录同一账号后可按日期分组查看、切换和继续对话。
4. 用户从历史会话打开旧的操作卡片时可以查看结果，但不能执行已过期的确认动作。
5. 用户输入 `/搜索 Unity API`，系统执行正常的收藏库优先检索；输入 `/存入 https://github.com` 时进入可编辑收录预览。
6. 用户提交 `https://github.com`，系统提取核心信息、生成分类和标签，用户修改后确认入库。
7. 用户一次提交多个网址，其中包含重复、无效和无法抓取的网址；系统逐项显示状态，其他有效项仍可保存。
8. 用户询问“有没有 Unity API 文档”，资料库命中时返回当前账号已收藏的网站并标注“收藏库”。
9. 资料库无合适结果且允许联网时，系统通过该账号的当前 Provider 返回真实搜索结果，标注来源并支持预览收录。
10. 用户说“把 GitHub 改到开发分类并置顶”，系统只展示差异预览，确认前数据库不变化。
11. 用户可选择当前筛选命中的全部网站，或只选择当前实际已加载的网站；系统冻结带版本的选择快照并展示对象和影响范围。一次明确确认后按每批最多 100 条串行删除，每批均原子校验；任一批发生版本变化时该批不删除，并停止尚未提交的批次要求重新选择。
12. 同一个网站可加入两个 Space；删除其中一个 Space 后，网站本身和另一 Space 的关系保持不变。
13. 用户在 Space 中调整顺序并一键打开；若浏览器拦截部分页面，界面能指出未打开项并允许重试。
14. LLM 或搜索 API 未配置时，站内关键词搜索、浏览和手动管理仍可使用。
15. 浅色、深色、跟随系统三种主题均可切换并保持布局稳定。
16. 设置中心正确展示 OpenAI、DeepSeek、通义千问、Kimi、Ollama、Tavily、Jina、Exa 的图标和连接状态，图标在明暗主题下均清晰且不引起布局跳动。
17. 桌面端关键流程可在 Windows、macOS、Linux 的现代浏览器访问，不依赖浏览器扩展；Chrome、Edge、Firefox 和 WebKit 检查无阻塞问题。
18. 用户上传包含 2,541 个书签 occurrences 的提供样例后，解析预检能稳定复现相同计数、目录树、顺序、唯一 URL/重复/不支持统计；未完成第一次确认前，资料库无任何新增或修改。
19. 同一 URL 在多个源目录、以不同标题重复出现时，最终只关联一个 Site，但每个 occurrence 的目录路径、标题和顺序都保留；已存在 Site 的分类、标签和描述不会被静默覆盖。
20. `?q=a#one`、`?q=a#two` 和 `?q=b#one` 保持三个不同 identity；参数顺序或 fragment 不得在规范化、预览和入库中被删改。
21. 导入文件中的 favicon、`javascript:`/`file:`/浏览器内部页、无效项和 HTTP(S) 私网地址被分别处理：favicon 丢弃，不支持项带原因显示，私网地址可选择收藏但服务端没有发起抓取。
22. 低置信度目录簇才进入 LLM 批量分类；敏感 URL 不出现在模型请求中。无 Provider、预算为零、预算耗尽或 Provider 失败时，任务可降级完成并把无法分类项放入“未分类”。
23. 用户刷新或关闭页面、服务重启、取消或重试导入任务后，进度能够恢复且已完成分片不重复；重复确认和相同任务重放不会新增重复 Site/occurrence，跨账号无法查看或控制任务。
24. 100,000 occurrence 合成文件通过第 10.1 节性能门槛；预览使用聚合和分页，浏览器、Agent 上下文和 API 单次响应均不承载全量条目。
25. 页面 HTML 返回 403、超时或反爬响应时，系统仍可独立尝试站点根路径图标；页面声明图标与四个根路径候选均失败时只显示本地默认图标，且全程没有请求第三方 favicon CDN。
26. 网站存在安全 `preview_url` 时，详情页只展示紧凑预览；点击可放大，并可通过关闭按钮、点击背景或 Escape 收起，加载失败后不留下空白区域。
27. 用户在资料库向下滚动时，置顶与普通列表在接近末尾前分别静默加载下一页；切换筛选或重复触发 Observer 不会插入重复网站或应用陈旧响应。

## 12. 已确认的关键决策

- 网站、分类、标签、Space、RAG 索引、模型与搜索配置均归账号独立所有。
- 对话记录按账号保存在服务端，支持多会话、跨设备同步和按最后活跃日期分组。
- Slash Command 作为辅助快捷入口，MVP 提供 `/搜索` 与 `/存入`，并保留可注册的扩展接口。
- 站内结果不足时默认自动联网，用户可切换为“仅收藏库”。
- MVP 手填模型名，自动拉取模型列表与能力检测进入增强版本。
- 模型 Provider 首批内置 OpenAI、DeepSeek、通义千问、Kimi、Ollama 和自定义 OpenAI-compatible；模型与搜索厂商均配套品牌图标。
- Space 批量打开提供“新标签页 / 新窗口”选择，但最终形态受浏览器策略约束。
- WebHub 是完整的网站系统：使用全站 Header、独立 URL 与正常网页文档流，不采用原生桌面客户端式全高侧栏和固定窗口 Shell。
- Chrome、Edge、Firefox、Safari 的 Netscape Bookmark HTML 大规模导入属于 P0，不与“最多 50 个 URL”的普通抓取队列混用。
- 浏览器源目录树和每次 occurrence 是需要长期保留的来源事实；WebHub 分类是用户可编辑的业务组织，两者不能互相覆盖。
- 自动分类采用“目录簇与本地规则优先、低置信度才批量调用 LLM”的成本模型，并在分析前明确预算和降级方式。
- `import-browser-bookmarks` 作为网站内置版本化 Skill 提供给内部 Agent；服务端导入管线是解析、安全、确认和幂等行为的唯一事实源。

## 13. 技术原则（非实施计划）

在不造成明显绕路的前提下，技术职责初步划分为：

- **Next.js + React + Vercel AI SDK**：Web 界面、流式聊天、生成式 UI 和人工确认组件。
- **LangChain / LangGraph**：Agent 工具编排、有状态流程、写操作预览与 Human-in-the-Loop 中断。
- **LlamaIndex**：网站资料的索引、混合召回与 RAG 上下文构建。
- **ReactBits**：少量高价值交互动效，不承担基础组件体系。

实施时使用届时的稳定版本，不沿用过时教程中的固定主版本。当前官方资料显示 Vercel AI SDK 最新文档为 v7，其流式 UI、工具审批和生成式 UI 能力与本项目吻合。

技术参考：

- [Vercel AI SDK 文档](https://ai-sdk.dev/docs/introduction)
- [LangGraph 概览](https://docs.langchain.com/oss/python/langgraph/overview)
- [LlamaIndex Framework](https://developers.llamaindex.ai/python/framework/)

## 14. 版本优先级

### P0：可用 MVP

- 账号注册、登录、会话、密码修改和账号级数据隔离
- 服务端多会话历史、跨设备同步、日期分组和 Slash Command 扩展框架
- Agent 对话、站内检索与真实联网搜索
- 单个/批量收录、核心元数据提取、编辑预览与确认
- Chrome、Edge、Firefox、Safari Netscape HTML 大规模书签导入：源目录/occurrence 保留、两阶段预览确认、自动分类、可恢复任务和内置 Skill
- 网站、分类、标签、Space、置顶和手动管理
- OpenAI、DeepSeek、通义千问、Kimi、Ollama、自定义模型配置，以及 Tavily、Jina、Exa 搜索配置与连接测试
- 模型与搜索 Provider 品牌图标和完整明暗主题状态
- 桌面端跨平台 Web 布局与主流浏览器兼容
- Windows 主机本地启动与家庭局域网访问
- 浅色、深色、跟随系统主题
- 数据导出与自有备份导入

### P1：增强版本

- 手机和平板响应式布局、触控交互和移动端导航抽屉
- Docker Compose、NAS 与 Linux 常驻服务器部署
- 网站预览截图
- GitHub 仓库及其他关联站点识别
- 浏览器专有 JSON/同步 API 等 Netscape HTML 之外的导入格式，以及跨设备增量书签同步
- 基于历史修正学习目录映射、跨文件智能合并建议和更高级的分类治理
- 标签同义词合并和批量治理
- 搜索 Provider 可配置降级顺序
- 更完整的模型自动发现与能力检测

### P2：远期能力

- 家庭共享工作区、跨账号协作和细粒度角色权限
- 公网部署、邮箱验证、OAuth、限流与审计
- 多设备云同步和协作分享
- 网站健康巡检与失效提醒
- 多 Space 模板和自动化工作流
