# WebHub 当前进度快照

**这份文件必须每轮迭代更新。** 它的唯一职责是让任何新会话（丢上下文、换账号、换 Agent）
在 3 分钟内知道「现在能跑什么、不能跑什么、下一步做什么」，而不必翻 commit 或读代码。

- 调度看 `ITERATION_QUEUE.md`（唯一入口，从上往下取第一个「待做」）
- 本机环境先看 `LOCAL_DEV.md`（不进 Git、无敏感值；真实账号与环境细节按其中路径进入 temp 隔离说明）
- 历史架构基线看 `IMPLEMENTATION_PLAN.md`（冻结于 2026-07-26，不参与当前调度）

最后更新：2026-08-30 · Q41 完成 Agent 来源信任、URL 安全与持久化容量硬化，最终组合门禁已通过

---

## 一句话状态

Provider 配置、Agent 增改查、书签导入、网页抓取、批量入库、自定义排序、Space 一键全开、
混合检索、LLM 全库重分类九条链路**已建成**。Q14 已完成设置页分区、站内网站详情导航，
以及 Agent 流式 Markdown、可折叠 reasoning 与真实 Provider usage 展示；Q15 进一步完成多候选
真实 favicon、紧凑可放大预览、最多 100 条原子批量删除和资料库静默自动分页。Q16 补上历史
书签的数据库驱动元数据扫尾、导入后小切片优先、同源 favicon 缓存和页面可见范围内的有界刷新。
Q17 把用户主动的全库补全改为持久化任务：固定目标集、常数开销进度、刷新/重启恢复、同源串行与受控
后台并发；同时保护用户主动清空字段、修复遗留 Google S2 图标，并避免无 `og:image` 的页面被反复抓取。
Q18 将分类、标签、简介、详细介绍收敛为四个受限 LLM 站内工具：模型只产出内存草稿，服务端在 Provider I/O
后重验账号 taxonomy 与字段来源并原子回填；批量任务增加模型 2 路上限、租约失效取消、Provider 前复核，
以及“冷却 + 连续三次失败才熔断”的持久保护。最终状态机复核又补齐 item attempt fencing、熔断与所有
收尾操作串行、Provider 信号先行持久化、付费调用返回后的短提交取消保护与本地落盘异常熔断；声明了
预览图但临时验证失败的页面不再被永久误记为“已检查”。
Q19 将明确的 URL 收录意图改为服务端确定性草稿，不再依赖模型选择工具，并补齐 Agent 来源归因、
确认状态持久化、批量部分失败与首页即时刷新。单站详情新增独立“AI 分析入库”动作和结构化结果，
同时修掉 SQLite 旧快照、历史 marker 提前返回和失败无限重排；首页批量任务会区分正常完成、部分失败与
Provider 提前熔断。旧 API 404 已改为可操作提示，本机数据库已升级到 `20260728_0011`，API 使用热重载进程。
真实单站复现又发现 LangGraph 分支回调引用了仅在函数局部导入的 `MessagesState`，导致构图阶段
必然 `NameError`、模型请求实际从未发出；该注解作用域错误已修复，同类 `NameError` 也不再作为
暂态 Provider 故障重复消耗批量重试。修后最小工具图与普通对话均已真正到达当前启用 Provider，
但两者都返回 HTTP 503；模型目录读取成功且已列出当前模型，因此剩余阻塞属于上游生成服务，需切换
可用 Provider 后继续页面验收。
Q20 为国外站点、反爬页面和正文不足页面补上搜索证据兜底：只在原网页证据不足时查询账号启用的
搜索 Provider，最多取 3 条目标域名结果，再交给分类、标签、简介和详细介绍工具。Tavily、Jina、Exa
现在可由用户显式执行一次可能消耗额度的最小真实搜索测试；新增“Exa MCP 免费额度”作为无需 API Key、
必须主动启用的低频选择，只允许 Agent 对话和单站手动分析。批量来源绝不调用免费搜索；原页证据不足时
只把当前网站记为受限并继续整批，只有专业 Provider 可做批量搜索补证。外部能力继续使用白名单、
类型化适配器，不执行任意 GitHub Skill。**Q20 迭代当时只完成代码与静态检查；当前全仓自动门禁
已在 Q26 收尾通过，真实搜索请求与浏览器验收仍未执行。**
Q21 修复免费 Exa MCP 被统一 8 秒外层截止提前取消的问题：专业 HTTP 搜索仍为 8 秒，免费 MCP
内部总预算为 16 秒、外层监督窗口为 17 秒，并省去关闭会话时的额外网络请求。搜索证据过滤允许目标
域名的官方子域，仍拒绝相似后缀域。网站资料新增独立 `summary` 字段与 `0012` 迁移；LLM 简介严格
20-50 字，详细介绍严格 100-300 字，分别通过四个工具产出并保留逐字段人工/LLM 来源保护。原网页
超时但搜索补证与 LLM 成功时现在记为受限成功，不再把已写入的分析误报为整次失败。
遗留 nullable `summary` 已用不重建 `sites` 的原生迁移收紧为 `TEXT NOT NULL DEFAULT ''`，2027 条旧记录
零空值且当时 Alembic 无 schema 漂移；Q24 又将本机数据库升级到 `20260729_0013`，增加 Space 批量操作
持久回执。Web/API 监听 3100/8100；页面和真实 Provider 结果仍由用户手动验收。
Q22 保留网址库默认居中阅读宽度，同时增加可持久化的铺满模式和宽度过渡；该选择只在两种宽度确实
不同的大屏显示，刷新时先静默恢复偏好，避免非用户触发的整页伸缩。编辑弹窗改为视口居中并具备完整
进出场生命周期，写入期间禁用所有退出路径，原生意外关闭也会重新同步受控状态。所有用户可见
“资料库/收藏库”统一为“网址库”。Agent 思考折叠区移除受控 `details` 的 toggle 状态反馈环，流回调、
滚动与工具事件均做稳定化和去重；生成期间先停止再允许切历史/新对话，历史读取使用请求序号隔离，旧
transport 错误和工具状态不会串入新会话。
新增只读 `present_website_recommendations`，最终推荐由模型提交结构化清单，服务端校验 URL、去重并回查
当前账号；新消息带推荐协议版本，前端只读取当前问答中最后一次最终清单，漏交清单时不会把原始搜索候选
冒充推荐；已收录网站进入站内详情，未收录网站保留收录/打开卡片，站外来源使用真实搜索 Provider 名。
Q23 允许用户在新增/编辑网站时直接新建标签，成功后立即选中并同步到网址库或详情页；用户与 Agent
继续共用账号级 `tags/site_tags`，浏览器、确认链和后端草稿统一使用 NFKC、折叠空白与大小写无关的身份。
并发或规范化同名创建返回 409 时会重读标签并复用现有项；Agent 被明确要求优先复用已有同义/近义名称。
近义判断目前仍由模型结合现有标签完成，没有在缺少用户确认样本时直接引入 embedding 固定阈值。
YouTube、youtu.be 与 youtube-nocookie 的受审计 URL 形态增加严格平台媒体回退：使用 YouTube 官方
favicon，并仅在 11 位视频 ID 完整合法时生成 `i.ytimg.com` 封面；真实网页声明始终优先。这不是通用
第三方 favicon 服务，也不会把任意网站域名发送给图标 CDN；若浏览器仍无法读取官方 favicon，界面使用
站内播放标识，不再退回字母块。网址库“居中/铺满”当前项改用产品绿色高亮。
Agent 原本已从后端逐块产生 SSE，浏览器整段出现的根因是 Next 外部 rewrite 对短 SSE 启用 gzip 后缓冲；
响应现带 `Cache-Control: no-cache, no-store, no-transform`，阻止代理改写流。截图中的 NodeSeek 后端实际已
在约 146 秒后完成 LLM 回填，长期转圈来自详情页一次性 GET 与网址库仅 32 秒的陈旧 UI 快照；两处现复用
约五分钟有界刷新，后段间隔封顶 15 秒，并在页面聚焦/恢复可见时补读，终态后停止。新站显式分析进入单个受控交互溢出槽，
降低被历史批量挤掉的概率；但队列彻底满或 API 进程崩溃时，本次单站 LLM 意图仍没有持久化任务保证。
Q24 新增 `propose_space_batch` 与 `POST /spaces/member-batches`：Agent 可生成纯创建 Space、创建并加入
多个网站或向已有 Space 批量加入的单张草稿，用户只确认一次。确认接口先校验全部网站账号归属，再在
同一事务中领取一次 Space 版本并写入所有缺失成员；全部目标已存在时允许安全重放，其他陈旧版本整批
409，不产生部分写。旧 `propose_space_membership` 已从工具 schema 和运行时两层禁止新增，只保留移出，
不会再退回逐站确认。批量卡可逐项剔除、恢复和恢复全部；后续 Space 批量方案会冻结所有旧的未完成卡。
create 和 existing 都必须使用本轮服务端命名空间工具 ID 作为 `operation_id`；不可变数据库回执绑定
账号、完整载荷、目标、首次选择的网站及响应结果，原载荷可重放而不同载荷会明确冲突。create 还以该 ID
派生确定性 Space ID，因此空 Space 也可安全重试；`noop/rejected` 同样持久化 tombstone，旧方案不会复活。
确认严格绑定原会话、真实落库的 Agent 回合、最新草稿、目标、候选子集及操作回执，动态名称只作为低权限
事实回放。最新未确认草稿可理解“不要某网站”并生成完整替代清单；确认后的最新草稿不再回放，也不会
向前复活旧方案。Space 创建、重命名、删除、移出弹窗
统一复用网址编辑弹窗的居中、160ms 进退场、遮罩/Escape 和写入期间关闭锁，并补上拖拽越过遮罩时的
误关闭防御。
Q25 定位截图中的“打开 1/3”为普通网页 `window.open()` 的用户激活配额，而不是 Space 成员缺失。
新增 `apps/browser-extension` MV3 助手，同一包可侧载到 Chrome 或 Edge：限定本机 WebHub origin 的
content script 只负责严格消息桥，service worker 在来源窗口创建非活动标签，全部成功后调用
`tabs.group` 与 `tabGroups.update`，以 Space 名设置绿色展开分组，再激活第一项。网页端先做能力握手，
连接时以原生分组为主动作，未连接时才保留普通新标签/当前标签降级。操作 ID 与无 URL 哈希回执避免
响应丢失后重复开组；首个浏览器副作用前即持久化 pending 回执，新标签先打开扩展自有占位页并自行登记
tab ID，再导航到目标网址，因此 Worker 在创建回调缝隙中断也能恢复。自登记、恢复、过期清理和结果落盘
按 operation hash 串行；同载荷并发除复用结果外，还为每个 operation ID 写完成回执。创建、分组或回执
失败均逐标签回滚；不同页面任务全局串行且最多积压 3 项。前端和扩展双重限制单次 100 个标签，原先
静默拉取最多 500 条的逻辑已移除，超过上限会明确拒绝，避免数百上千页面拖垮浏览器。最终竞态收口又让
网页以 Web Locks 串行同一 Space 的任务预留/清理，并短期保存原名称、规范化 URL 和任务创建时间；Space
改名或成员变化后会先恢复旧任务，而非丢失载荷或另开一组。未确认身份和完成回执统一保留 7 天；扩展
使用持久化启动轮次，在普通扩展 reload/update 时保持、只由 `runtime.onStartup` 在浏览器启动时轮换，既能
继续清理扩展重载前的真实任务，又不会跨整机重启删除可能复用的 tab ID。完成回执重放前还会通过
`tabGroups.query` / `tabs.query` 核对同名绿色组与标签数量；恢复 alias 只可匹配本任务开始后完成且仍实际
存在的同载荷结果，查询异常会保留回执而不是误判为不存在。正常的新一轮打开不会误复用较早分组；若
浏览器恰在标签已创建但 completed 尚未写入的短窗口退出，扩展会返回 `CROSS_SESSION_PENDING` 并暂停
自动重建，避免在无法证明旧 tab ID 归属时误删普通标签或静默生成重复组。
Q26 为 Agent 每次提交加入稳定 `turn_id` 和账号级持久回执：Provider 前原子写入用户消息、Assistant
占位与回合绑定，流中有界检查点，最终以 `complete/error/aborted` 保存部分正文、reasoning、工具结果、
可信来源和真实运行指标。同 ID 同载荷不重复执行，异载荷冲突，60 秒租约配 15 秒心跳并在启动/请求入口
回收过期回合；消息与回执终态原子提交，旧执行器不能越过 fencing 覆盖结果。Tavily、Jina、Exa 的受信
HTTP(S) 结果提升为 AI SDK `source-url`，规范化去重并保存实际 Provider，前端以折叠来源区展示证据。
历史恢复四态、未完成草稿禁用、上滚脱离自动跟随、停止/异常工具状态收口、reasoning 计时、真实 Token、
复制回答/链接与全站共享 Spinner 已接通；未完成站外推荐不能收录，极早停止会立即收口，已授权会话中的
Slash 协议错误也受同一幂等回执保护。完整门禁收尾还修复了 ingestion worker 以裸对象 ID 永久记录
停止数据库导致的 ID 复用误判：当前用弱引用与严格对象身份围栏，TestClient/数据库关闭不再让后续实例
假性停机或挂起。删除会话前会解除回合回执的会话/消息外键并保留最小 tombstone，完成态和执行中回合的
延迟首轮重试均不会再次调用 Provider；复制会话链接从 `origin + pathname` 重建，只保留受控 `c` 参数。
严格全量门禁又定位并修复自动回填关停时取消尚未入池的 SQLite 连接初始化：consumer 现在先排空受保护的
discovery/session 清理任务，再传播取消，并在查询返回后重查数据库停止状态，不再把 aiosqlite worker 留到
事件循环关闭后才由 GC 回收。
`20260729_0014` 已升级到 head，Web/API 已重新监听 `3100/8100`。
Q27 将 Provider 预检失败从统一的“未配置”拆为六类固定安全契约：确实未启用、配置不完整、密钥
不可解密、Clash/Mihomo Fake-IP、SSRF 目标受阻和 DNS 不可用。`198.18.0.0/15` 仍被严格拒绝，
但设置页连接测试、Agent 实时流、持久错误消息与同 `turn_id` 重放现在保留精确错误码；厂商异常文本、
Base URL 与密钥片段均不进入响应或数据库。前端按错误类型提供配置、密钥或连接测试入口，未知错误不
生成误导操作。该诊断只作用于 Provider 连接校验，不改变 favicon/网页抓取的现有错误边界。
Q28 修复了推荐工具参数失败被误报为“旧版本”的问题：LangGraph 工具异常不再把原始 kwargs 字符串
写入消息，最终展示工具的内部重试也不再渲染成通用工具卡。`search_library` 新增账号隔离的精确
`category_id` 与 `include_all`，完整结果只冻结在本轮服务端结果集中；模型仅看到最多 20 条预览和
随机 `result_set_id`，`present_website_recommendations` 再通过 LangChain artifact 把完整轻量清单
交给 UI 并持久化到现有消息 `sources`，因此刷新和同 turn 重放不丢结果，也不会把 87 条重新塞进模型
上下文。结果区每页固定 12 条，提供数字页码、上一页/下一页、首尾禁用与切换结果后回到第一页。
Q29 在书签预览中增加确定性的同 authority 页面聚类和显式用户决策：不使用 embedding/LLM，不猜公共
后缀，保留端口并排除共享内容平台；设置页分页展示推荐主页、依据与成员，未决组阻止 apply。最终入库
改为 job/decision 双版本、单事务 CAS，失败零部分写入且并发只允许一个成功。分类优先复用账号可信历史、
精确同名文件夹分类和本地多信号规则，并从文件夹生成有限标签，不再要求逐站消耗模型额度。
Q30 将首页补全拆成默认 200/最大 500 且绝不调用 LLM 的“快速补全”，以及默认 50/最大 100 的
“LLM 完整分析”；启动前精确预估可处理、选中和模型调用数量。批量模型并发收紧为 1、关闭 SDK 内部重试，
Provider 冷却改为安静等待并持久化安全停止原因。熔断后只把实际领取过的条目记为失败，未领取条目记为
跳过；`0016` 已把历史 2028 项任务从错误的 2027 个失败修复为 1 个受限、5 个失败、2022 个跳过。
Q31 在网址库增加全账号相似网站排查：扫描完全使用本地 URL 规则，不调用 LLM、embedding、搜索或外部
网络；严格区分可折叠 URL 变体与普通同站页面，默认所有组完整保留。卡片可逐项取反并保留多个网站；
空选择显示“全部保留”，非空显示“保留所选”且只清理未选成员，也可一次选择当前 Tab 内全部推荐项。
分组支持总页数、数字页码直达与前后翻页，底部执行栏始终浮在滚动内容上层。执行前再确认，并用网址库
指纹与数据库级决策版本 CAS 阻止陈旧或并发页面写入。被删网站的标签、置顶、Space 与导入来源只汇入
确定的主保留项，其他已选网站保持独立，失败不会产生部分删除。持久快照可刷新恢复并有界清理旧版本。
Q32 修复两条被持久层割裂的真实流程。网站资源域名被 Clash TUN 映射到 `198.18.0.0/15` 时，不再直接
误报内网，也不放行 Fake-IP；只有域名的全部系统 DNS 答案都是 Fake-IP 才通过固定 TLS/SNI 的独立公网
DNS 复验，完整公网地址集合继续过原 SSRF 规则并固定真实 IP 连接。直接 IP、混合答案、私网答案和每个
重定向仍严格拒绝，正常远端服务器 DNS 路径不变，Provider 预检仍沿用 Q27 的 Fake-IP 诊断。
Agent 现在只把最近一次成功外部推荐中的未入库名称/URL 投影成最多 12 条低权限事实，URL 再校验；已
入库项、敏感/私网 URL、站内 87 条等大结果与任意工具原文均不回放。下一轮“把刚才推荐的三个收藏”可
据此生成 `propose_sites` 草稿，但仍须用户确认且不能猜网址。

Q33 对现有库上再次导入大量书签做了隔离容量审计：10k / 50k / 100k 均可到达解析预览，分别约
4.1-4.4 秒 / 33-35 秒 / 77-79 秒；但 100k RSS 峰值约 182-400 MiB、SQLite 增量约 347-393 MiB，
深层祖先链存在 O(k²) 退化，最终 apply 仍是长 SQLite 写事务，终态任务也没有 GC。因此几千到约 10k
可以继续使用，50k 起需谨慎，100k 尚不能承诺完整可靠导入。Q34 为 `/settings/import` 增加全窗口文件
拖放：文件拖到 Header、正文或浮层上均显示顶层提示并复用原上传链；文本/链接不拦截，多文件和非 HTML
前端拒绝，忙碌时阻止浏览器打开文件且不启动第二个任务。点击选择入口仍保留。

Q35 统一了书签相似组数字分页，单组决策改为原位更新当前页，用户在第二页处理后不再跳回第一页。
Agent reasoning 在流式开始边沿自动展开、完成边沿自动收起，普通渲染不覆盖用户之后的手动开合。
单站“AI 分析入库”新增数据库真实阶段：抓取网页、整理证据、等待模型、调用模型与保存结果由后端发布，
前端轮询展示，终态清空；不使用定时伪进度，也不改网站的用户可见更新时间。性能侧只把模型信号量缩小
到实际 enrichment 调用，Provider 并发仍为 1，网页抓取可并行，favicon 与 preview 验证也并行执行。

Q36 将首页排序、网址库标签/排序、网站分类、Provider 模型和 Space 排序六处原生下拉统一为 DOM
主题选择器，展开层不再由 Windows/浏览器绘制；支持深浅主题、禁用项、搜索、完整键盘操作、视口碰撞、
原生 dialog/body Portal 与焦点恢复。Provider 模型列表可搜索并保留手填回退，原生 datalist 已移除；
两个裸复选框改用站内主题。控件审计同时补齐 Agent 历史菜单导航、Slash 候选框关联和账号菜单 Esc 收口。

Q37 将首页右上角搜索入口与网址库真实输入框接成一个受控共享元素：鼠标点击时先提交源框，再切换路由，
搜索框用 340ms 缓动移动并扩展到网址库工具栏，周边页面只做短淡入。网址库区不再渲染右上角重复入口；
`Ctrl/Cmd+K`、键盘点击和减少动态效果走快速聚焦。协调器包含预取、重复触发锁、超时导航保证、焦点接管、
Header 上层投影和居中/铺满首帧宽度锁定，网址库列表数据不会阻塞转场。

Q38 收敛首页 Agent 输入框的双层高亮：全局 textarea outline 不再与外层 focus-within 重复绘制；Hover 只
增强中性边线，聚焦改为 42% 强调色混合边线与 8% 柔光。追问输入框同步使用同一视觉语言，系统高对比模式
仍保留原生 Highlight 轮廓，不改变 Agent 输入、快捷键或提交行为。

Q39 收紧 Agent 的检索范围语义。`仅网址库` 不再挂载 `web_search`，站内无命中时返回可识别的联网切换
信号，服务端拒绝模型凭记忆提交的库外推荐；前端展示“开启联网搜索”按钮，点击后切换为 `允许联网`，
复用最近用户问题和当前 `conversationId` 重新执行。`允许联网` 仍先查网址库，只有不足时调用本轮真实
搜索 Provider，外部卡片必须命中本轮搜索 URL；旧 `llm推荐` 历史显示为“模型知识（未实时验证）”。

Q40 修复网址库完整推荐被模型预览截断的问题。`search_library` 默认冻结全部匹配结果，使用不调用
LLM、Embedding 或网络的确定性相关度排序；最终推荐通过 `result_set_id` 把完整结果交给现有 12 条/页
数字分页。若模型误把有限预览传回，服务端会在同一轮最新检索仍为全量时，从本轮最近且覆盖这些条目的完整集合恢复
结果；显式 `include_all=false` 的精选请求不会被扩大。新增 105 条全量结果、默认参数和预览恢复回归测试。

Q41 把推荐与来源合同升级到 manifest v3，同时只读兼容合法 v2 历史。v3 正文/reasoning 外链必须精确
命中同消息受信 `source-url`；旧 `llm推荐` 显示为“模型知识（未实时验证）”，凭据、私网/本机、敏感
query/fragment（含 hash-router 参数）等兼容 URL 与 favicon 由前端启发式过滤。Provider URL 在工具和
runner 两层 canonicalize/filter/dedupe，拒绝非公开目标、凭据、敏感参数、编码/非规范数字 IPv4、
组播地址、畸形 IPv6、单标签主机、`home.arpa` 等私有/保留后缀、反斜杠和畸形 URL；已持久化的旧
`web_search` 来源在重放时重新校验，孤立 surrogate 或非有限 JSON 数值 fail closed；Provider 的流式
text/reasoning 在入流和入库前做 UTF-8 安全投影。默认“仅网址库”，显式联网后仍先查库，
`web_search` 必须复用最近一次完全相同 query；
站内私有字段外发过滤只是 heuristic，不是 DLP。

Q41 同时给回合持久化设定可验证预算：推荐 artifact 256 KiB，持久 `sources` 384 KiB，持久
`source-url` parts 64 KiB；过大普通结果改存 `persisted_tool_result_truncated`，可执行 Space/写入草稿
优先保留。最终推荐和 `source-url` 先经持久化接纳再发送；较早已实时显示的可重建只读中间结果在聚合
超限时可只以摘要进入历史/重放，因而不承诺完整 live 序列与 replay 逐事件一致。显式 N 条由服务端按
确定性查询相关度冻结并以 `result_set_id` 交付；未指定/全量也走该排序，
只有无明确数量的主观有限精选仍按 `updated desc`。Agent 不使用 Embedding。105/22 只是分页样例；当前
没有任意规模或 100,000 条 Agent 结果分页 API，完整结果超过 256 KiB 返回 `result_set_too_large`。

当前自动化证据：**前端完整 222/222**、Q41 受影响后端六文件 **127/127**、后端完整 **708/708**；
完整 Ruff/ESLint、TypeScript、Next.js 生产构建（静态页 **12/12**）与根级 `pnpm check` 均已通过。
开发库此前已实查为 `20260731_0019 (head)`；本轮未执行真实相似扫描/清理或大文件 apply，未调用真实
LLM/搜索 Provider，也未修改 Clash TUN 或 Provider 配置。未运行 Playwright，页面行为仍由用户手动验收。

---

## 能跑的（已实测，不是「代码存在」）

| 能力 | 状态 | 实测证据 |
| --- | --- | --- |
| Provider 配置页 | ✅ | 浏览器实测：三分区、掩码、启用互斥 |
| 连接测试 + 拉模型列表 | ✅ | 真实 DeepSeek：「连接成功，读取到 2 个模型」 |
| Base URL 自动预填 | ✅ | 选 DeepSeek 自动填官方地址 |
| 模型名下拉选择 | 🟡 | 真实拉取与选择流程原已通过；Q36 改为可搜索主题选择器并保留手填回退，新外观待用户验收 |
| 全站主题选择器 | 🟡 | 六处原生下拉与 Provider datalist 已清零，静态/编译/构建门禁通过；待真实浏览器验收深浅主题、键盘和移动端 |
| 首页到网址库搜索转场 | 🟡 | Q37 共享 layoutId、预取、焦点/超时/宽度竞态与降级门禁已通过；待真实 Chrome 验收移动连续性和落点 |
| 首页 Agent 输入焦点视觉 | 🟡 | Q38 已移除内外双重高亮，统一柔和外层焦点与高对比降级；待真实浏览器确认深浅主题观感 |
| Agent 对话 + 站内检索 | ✅ | 真实模型回话，来源标注正确 |
| Agent 检索范围与联网切换 | 🟡 | Q39 后端范围/来源门禁、同会话切换按钮与契约测试通过；仅网址库无命中提示、联网优先查库及真实 Provider 结果待用户验收 |
| Agent 网址库完整结果与分页 | 🟡 | Q40 默认全量冻结、确定性相关度排序、预览误传恢复和 105 条结果回归通过；现有前端 12 条/页数字分页已接通，待用户手动核对 105/22 条结果无遗漏、刷新恢复和页码跳转 |
| Agent 来源、URL 与历史容量硬化 | 🟡 | Q41 manifest v3、同消息 source-url 外链白名单、Provider URL 双层过滤、三档持久化预算、显式 N 条冻结和草稿优先级已接通；前端 222/222、后端 708/708 与最终组合门禁已通过，仍需受控假 Provider 与真实浏览器验收 |
| Agent 收录网站（propose→确认） | ✅ | 确认前 0 条 → 确认后 1 条 |
| Agent 改网站（改名/分类/标签/置顶） | ✅ | 只把真变化的字段进 diff |
| Agent 创建/批量加入/移出 Space | 🟡 | 新增走单张 `propose_space_batch` 草稿并一次确认，旧 membership 工具只保留移出；代码已完成，待用户手动验收 |
| 确认结果回写会话 | ✅ | 同一对话「收录→接着改」一次走通 |
| 书签解析 + 去重 + 预览 | ✅ | 真实 2541 条 → 2024 候选，1.08 秒 |
| 书签落库 | ✅ | created 2024 / failed 0，0.47 秒 |
| 书签导入前端入口 | ✅ | `/settings/import` 支持点击选择和全窗口 HTML 拖放；隔离浏览器已验证桌面/窄屏、离开恢复、非文件隔离和一次上传轮询，真实系统文件由用户最终验收 |
| 书签同站页面整理与分类 | 🟡 | Q29 迁移、API 与双版本事务已完成；Q35 统一数字分页且决策后保持当前页，待真实书签页面验收 |
| 书签导入 Agent 工具 | 🟡 | 后端三件套 + skill 已是真契约；前端无草稿契约与确认通道，只能转到 `/settings/import` 完成 |
| 网页元数据抓取 | ✅ | 建站后自动分析 + 详情页「重新分析」，SSRF 每跳复验；Q32 实测 Clash Fake-IP 下 `www.smzdm.com` 经独立公网 DNS 复验后返回 `complete` 并取得 favicon |
| 单站 AI 分析进度 | 🟡 | Q35 后端持久化真实阶段、前端轮询、模型信号量缩域和 favicon/preview 并行验证已完成；待真实 Provider 页面验收 |
| 首页有界网站资料补全 | 🟡 | Q30 双模式、精确预估、Provider 停止原因、历史统计修复和 817 项自动化门禁通过；`0016` 与健康检查实测，待真实小批次页面验收 |
| 网址库相似网站排查 | 🟡 | Q31 本地规则、持久快照、组内多选、分页决策、执行 CAS 与单事务清理已完成；`0018` 和健康检查实测，真实扫描与清理由用户验收 |
| 批量 URL 入库 | ✅ | URL 由代码抽取；预览只读、确认后逐项独立提交 |
| 分类内自定义排序 | 后端已测 | 迁移已在实库跑过并回填；**界面未浏览器实测**（API 进程当时已停） |
| LLM 全库重分类 (Q12) | 真实 Provider + Chrome 已测 | 2027 个网站总数不变，851 个网站各更新一次（`version_sum` 2029→2880）；分类内 position 零空值、零重复，标签、Bookmark、analysis、source、provenance 均不变 |
| 分类图标 | 真实 Chrome 已测 | 首页分类与网站卡片均渲染实际 Lucide 图标（含 AI 分类的 `Bot`）；按名称推断链路已生效，**没有前端选择器**，用户暂不能手选 |

## Q14-Q41 代码完成、待用户页面验收

> 下表保留各迭代当时的专项证据；截至当前，前端完整 222 项、后端完整 708 项、完整 lint/typecheck、
> Next.js 生产构建（静态页 12/12）与根级 `pnpm check` 已通过。

| 能力 | 自动化证据 | 用户需手动检查 |
| --- | --- | --- |
| Provider / 语义索引分区 | 路由、契约、费用估算测试 + TypeScript + 构建通过 | Provider 页不再出现书签/索引信息；账号菜单可进入独立语义索引页 |
| favicon 与预览抓取 | 页面声明 + 四个站点根候选、HTML 403/超时后独立图标抓取、SSRF/DNS 固定、重定向、generic MIME + magic、大小和编辑竞态测试通过 | 新增或改 URL 后真实图标是否逐步出现；页面受限但根图标可用时是否仍显示；全部失败时是否稳定回落本地默认图标 |
| 站内网站详情 | 首页、资料库、Agent 导航源码回归 + 紧凑预览/放大层交互测试 + 动态路由构建通过 | 各类已收录卡片先开站内详情；只有“访问官网”按钮外跳；紧凑预览点击可放大并能用关闭按钮、背景与 Escape 收起 |
| Agent 流式富文本 | UI Message Stream、reasoning、usage、历史恢复、Markdown 链接过滤测试通过 | 真实模型流式打字、reasoning 展开/收起、耗时/Token、表格/列表/代码及链接卡片显示 |
| 资料库批量删除 | 原有 100 条单批上限、账号隔离、版本冲突整批回滚、Space 版本与响应 ID 集门禁已覆盖；新增轻量筛选快照、“全选”/“全选（已加载）”双语义和跨批进度本轮仅做静态复核，未运行门禁 | 网格/列表选择态不导航不拖拽；“全选”数量等于当前筛选快照，“全选（已加载）”数量等于当前实际加载数且可超过 100；超过 100 条时确认一次并观察每批最多 100 条的连续进度，冲突后应停止剩余批次 |
| 资料库静默自动分页 | 置顶/普通独立哨兵、640px 提前量、重复与失败游标、请求取消、筛选切换、陈旧响应及无 Observer 降级测试通过 | 在真实 Chrome 向下滚动时自动接续下一页，无“加载更多”按钮、重复卡片或明显跳动 |
| Q16 历史元数据补全 | 代码静态复核：数据库驱动扫尾、原子 claim、4 个总并发槽/其中 2 个自动槽、16 条候选缓存、256 条普通队列、favicon 同源缓存与总超时 | 打开首页或资料库后，历史 `not_analyzed` 逐步开始补全；可见卡片无需手动刷新即可出现图标/说明；离屏分类不持续发请求 |
| Q17 首页批量元数据补全 | 代码静态复核：持久 run/item 快照、账号单活跃任务、run 原子计数（状态读取 O(1)）、版本/状态/claim 守卫、应用重启恢复、同源串行、后台共享 3 槽且保留 1 个交互槽；本机 `0011` 迁移和新路由就绪已确认 | 首页 Agent 面板下方点击“开始补全”，检查进度、成功/受限/失败/跳过计数持续变化，重复点击加入同一任务，刷新或重启 API 后任务继续，同域子页不并发抓取；历史 Google S2 图标应被真实图标替换或清除 |
| Q18 LLM 分类/标签/介绍回填 | 代码静态复核：四个闭合工具、12,000 字正文边界、逐字段 LLM 来源、I/O 后快照重验、`User -> Category -> Site` 锁序、模型并发 2、item attempt fencing、单调时钟租约截止、Provider 信号先行持久化、冷却与三次连续失败熔断 | 使用已启用的模型 Provider 运行首页批量补全；抽查原“未分类/无标签/无说明”网站是否得到合理分类、2-6 个标签、20-50 字简介和 100-300 字详细介绍；人工字段不得被覆盖，URL 改址后新目标可重新分析；刷新/重启后进度继续且不会突然并发请求大量模型 |
| Q19 Agent/详情页分析修复 | 明确收录意图走确定性 `propose_sites`；Agent 创建写入真实来源并刷新首页；确认 metadata 可从历史恢复；单站分析释放 SQLite 旧快照并返回真实 outcome / `llm_applied`；LangGraph 构图 `NameError` 已真实复现并修复，修后请求已到 Provider；批量终态保留错误与 Provider 熔断语义 | 切换到可用 Provider 后，在 Agent 输入 `http://randomiban.com/入库`，应出现根网址确认卡且确认后首页立即出现；刷新会话仍显示已保存；详情页点击“AI 分析入库”，核对成功/受限/失败提示颜色及分类、标签、介绍刷新；无效 Provider 的首页任务不能显示绿色完成 |
| Q20 搜索补证与免费受限 Provider | 代码静态复核：正文长度与拦截页判定、专业搜索 8 秒/3 条上限、目标域名过滤、批量能力门禁、真实搜索探针错误脱敏；`exa_mcp_free` 使用官方 MCP SDK、单 API 进程并发 1、5 分钟缓存和 60 秒失败冷却。Python 编译、定向 Ruff、前端 TypeScript 与 `git diff --check` 已通过 | 设置页分别检查专业搜索 Provider 与“Exa MCP 免费额度”；Agent 联网搜索、受限站点单站分析和专业 Provider 批量补证应走对应边界；批量来源不得调用免费 Provider，证据不足的当前网站记为受限后整批继续；搜索证据不能生成 favicon/preview |
| Q21 搜索预算与双层网站文案 | 代码静态复核：Exa MCP 16 秒内部总预算/17 秒监督窗口、无关闭 DELETE、目标子域过滤；`summary` 迁移、API、Space、卡片、编辑表单、四工具原子回填、批量缺失判定及人工保护已贯通；本机 `0012` 迁移、Alembic schema check、3100/8100 启动与 API readiness 已确认 | 用 linux.do 等受限站点分别验证免费 Exa 与 Tavily；顶部只显示 20-50 字简介，详情区显示 100-300 字介绍；原页超时但搜索成功应显示黄色受限成功，不再显示整次失败；手改或清空简介后重新分析不得覆盖 |
| Q22 网址库与 Agent 交互收口 | 代码静态复核：弹窗受控关闭与 busy 锁、宽屏居中/铺满偏好及无闪动恢复、流回调去重、`details` 单向状态、历史请求竞态隔离、全工具阶段提示、结构化推荐 URL 校验/去重/账号回查、当前轮次与最终清单选择均已贯通；本轮收尾仅执行 `git diff --check` | 编辑弹窗是否居中且进退自然，保存中无法提前关闭；网址库默认居中、宽屏切换铺满后动画与刷新记忆是否正确；长 reasoning 流不再闪现 React 错误；站内、联网和模型知识推荐是否都只用统一网站卡片，已收录卡进入详情，未收录卡可收录/打开 |
| Q23 标签、平台媒体与分析反馈闭环 | 源码静态复核：表单内创建标签、统一规范化和 409 复用；YouTube 严格 host/视频 ID 白名单与仅补空媒体；布局当前项绿色样式；SSE `no-transform`；详情/网址库五分钟级有界刷新、聚焦补读和交互溢出槽均已接通。NodeSeek 数据库记录确认后端已完成，原问题是 UI 快照陈旧；该迭代当时未运行测试、构建、浏览器或真实 Provider | 新建标签是否立即选中且 Agent 复用已有近义项；重新分析 YouTube 视频后是否显示官方图标和对应封面；布局当前项是否绿色；真实模型是否逐块输出；新建网站即使分析超过两分钟也应自动由转圈切换为终态 |
| Q24 Agent Space 批量任务与弹窗复用 | 源码静态复核：新工具只产出一张完整草稿；旧 membership 工具禁止 add；单事务接口含账号校验、一次版本 CAS、整批回滚及并发结果复查；create/existing 均以命名空间操作 ID 写入不可变回执，原载荷可安全重放且变更载荷会冲突；卡片剔除持久化、旧草稿 tombstone、逐消息 `turnPersisted` 门控、原会话确认 marker、首页 Space 刷新及最新 pending artifact 回放已贯通；确认以回执为事实源校验目标、候选及写入结果；四类 Space 弹窗复用共享受控 dialog。本机 `0013` 迁移已应用。该迭代当时未运行测试、构建、浏览器或真实 Provider | 让 Agent 新建空 Space、创建并加入多个网站、向已有 Space 加入多个网站；每次只应出现一张确认卡和一次写入。剔除/恢复候选后再确认，实际成员应与卡片一致；用自然语言说“不要某网站”应生成替代草稿并禁用旧卡。检查 Space 四类弹窗始终居中、动效自然且提交中不能关闭 |
| Q25 Chrome/Edge 原生 Space 分组 | 源码静态复核：MV3 manifest 只开放本机 WebHub content script、`tabGroups`/`storage`；页面握手、来源窗口、严格 schema、HTTP(S) URL 规范化去重、100 条上限、占位标签自登记、operation 级锁、同载荷 alias 回执、全局串行/3 项积压上限、逐标签回滚及 pending/completed 幂等回执均已贯通。超时后只允许同 operation 重试，普通 `window.open` 仅作未启动扩展任务时的降级。该迭代当时未运行测试、构建或浏览器 | 分别在 Chrome/Edge 侧载助手并刷新 WebHub；状态应显示已连接。对 3 个网站的 Space 点击分组打开，应一次生成 3 个标签和一个同名绿色组；助手停用后应显示未连接且普通模式仍如实提示弹窗拦截。超过 100 个成员时必须拒绝，不能只开前 100 个 |
| Q26 Agent 回合可靠性与来源 | 稳定 `turn_id`、账号级回执、Provider 前原子占位、60 秒租约/15 秒心跳、两秒检查点、消息与回执原子终态、过期围栏、四态重放、删除会话保留最小回执 tombstone、可信 `source-url`、未完成草稿/站外收录门控、极早停止、协议错误回执、stick-to-bottom、受控分享链接、工具时间线、真实 Token、共享 Spinner 与自动回填取消排空已贯通；`0014` 已应用。严格根级 `pnpm check` 全绿：前端 175、后端 561、两端 lint、TypeScript 与生产构建通过 | 流中上滚不被拉回；停止后刷新仍见部分回答且所有写入入口禁用；删除会话后同请求重试不重复消息/调用；分享链接不携带无关 query/hash；联网回答只显示真实来源；工具步骤、思考秒数、实际 Token、加载态和 reduced-motion 表现一致 |
| Q27 Provider 精确诊断 | `198.18.0.0/15` Fake-IP 专用错误、其余配置/密钥/DNS/SSRF 分类、连接测试零出站拦截、流式安全脱敏、持久错误重放和前端操作映射均有回归；等价全仓门禁分拆全绿：前端 177、后端 569、两端 lint、TypeScript 与生产构建通过 | 在可控的 Clash 订阅切换场景验证新回合明确提示 Fake-IP；保存全局 `fake-ip-filter` 并应用后，新回合恢复且旧失败回执保持原状态；设置页连接测试显示同类诊断 |
| Q28 Agent 完整结果与分页 | 精确分类筛选、账号内完整结果集、模型/UI artifact 分流、安全工具错误、持久历史恢复、12 条分页和 87 条边界测试代码已贯通；TypeScript、ESLint、定向 Ruff 与 diff 检查通过，未运行测试/构建 | 询问“把库里的所有 AI 类网站都发给我”，标题应为 87，实际应有 8 页（前 7 页各 12 条、末页 3 条）；逐页核对无重复/遗漏，刷新会话后清单仍在；不得再出现“旧版本”提示 |
| Q29 书签同站整理与确定性分类 | 严格 authority 聚类、共享平台排除、推荐主页依据、终态投影冻结、决策 CAS、单事务 apply、失败回滚、并发单成功、账号历史/文件夹/规则分类优先级、前端游标分页与未决门禁均有回归；前端 185、后端 606、lint、类型与构建通过 | 用真实书签检查相似组和成员分页；分别合并、保留与全部保留，核对预计新增和实际 Site；刷新/跨标签页旧版本应冲突；人工分类、端口与共享平台不得串类或静默合并 |
| Q30 首页网站资料补全有界执行 | 快速/完整双模式、精确计划、每批上限、单 LLM 并发、零 SDK 批量重试、持久停止原因、安全 Retry-After、冷却等待、第三次失败收尾和历史迁移修复均有回归；前端 189、后端 628、lint、类型与构建通过；`0016`、历史计数和两端健康检查实测 | 先用小上限验证快速补全不调用 LLM，再用可用 Provider 验证小批完整分析；模式/数量预估应与固定任务一致，刷新可恢复；限流时已领取项保留真实结果、未领取项显示跳过，界面不泄露厂商异常 |
| Q31 网址库全量相似排查 | 严格 URL 变体指纹、同站分区、共享平台/敏感目标排除、账号隔离快照、数字页码/游标兼容、组内多选取反、当前 Tab 批量推荐、决策/apply 原子 CAS、全库指纹、单事务关系转移、失败回滚、旧脏决定安全迁移与快照有界保留均有专项回归；前端 197 项、Q31 后端/迁移 28 项、两端 lint、类型、构建、diff 与两端健康检查通过；`0018` 已应用 | 打开网址库扫描真实数据，核对总页数、数字跳页、固定底栏、长内容和移动布局；同组连续选择两张以上卡片，再逐张取消并刷新恢复；右上角“全部保留 / 保留所选”应随集合切换；分别在三个 Tab 一键选择推荐并确认不会跨分区；二次确认返回时零写入；用少量测试组执行后核对只有未选成员被删，其他保留项独立存在，关系汇入确定主项且并发旧页面被拒绝 |
| Q32 Fake-IP 与推荐上下文 | 系统 DNS/独立公网 DNS 双路径、固定 TLS/SNI、完整 A/AAAA/CNAME 校验、逐跳 pinned 连接、直接/混合/私网失败关闭；最近一次外部推荐只回放未入库且可信的 12 条名称/URL，站内大结果和通用工具输出不回放。联合专项 93 项、后端全量 655 项、相关 Ruff、真实 `www.smzdm.com` 非 LLM 抓取和 API readiness 通过 | 点击 `www.smzdm.com` 的“AI 分析入库”，确认不再误报内网；随后让 Agent 推荐 NGA、Stage1、酷安，再说“把刚才推荐的三个收藏”，应直接出现含精确 URL 的单张确认草稿且确认前不写库 |
| Q33-Q34 大批量审计与全窗口拖放 | 10k/50k/100k 隔离解析预览基准、100k Site 检索/SQLite 锁审计和生命周期审计已完成；拖放纯函数 6 项、前端完整 203 项、TypeScript、ESLint、diff 与 1280x720/390x844 隔离浏览器验证通过。浏览器假身份和上传拦截未写真实库 | 用资源管理器把单个 HTML 拖到窗口不同区域，确认覆盖提示与现有预览；非 HTML/多文件只报错。大文件实际导入仍先控制在约 10k 内，50k 以上等待 finalize/apply 分段、任务 GC 和完整端到端门禁后再作为可靠能力使用 |
| Q35 导入分页、思考折叠与单站分析进度 | 相似组当前页保持、reasoning 流式边沿开合、持久真实分析阶段与终态清空、轮询结果权威边界、LLM 信号量缩域、favicon/preview 并行及 `0019` 迁移均有回归；前端 208/208、后端聚焦 88/88、迁移 17/17、TypeScript、ESLint、Ruff lint 与 diff 通过；后端全量因工具超时未形成新结论 | 在第二页决策后确认不跳页；真实 Agent 流开始自动展开思考、完成自动折叠且手动开合不被普通渲染覆盖；真实单站分析按抓取、证据、模型、保存等后端阶段推进并在终态停止，不显示定时伪进度 |
| Q36 全站主题控件 | 六处业务下拉与 Provider datalist 已替换为共享 ThemedSelect；搜索、禁用项、键盘、Portal、碰撞定位、焦点收口、Provider 复选框、Agent/账号菜单可达性及源码防回退门禁已接通；前端 212/212、TypeScript、全前端 ESLint、Next 生产构建与 diff 通过 | 在网址库重点核对截图中的排序弹层不再出现 Windows 蓝色原生高亮；逐页核对首页、网址库、网站弹窗、Provider 与 Space 的深浅主题、键盘、禁用项、长模型搜索、弹框 Esc 分层和移动端边界 |
| Q37 首页搜索共享转场 | 持久协调器、路由预取、340ms 共享 layoutId、周边淡入、网址库隐藏重复入口、键盘/reduced-motion 快速路径、680ms 超时导航保证、焦点与铺满宽度竞态均已接通；前端 215/215、TypeScript、全前端 ESLint、Next 构建与 diff 通过，3100/8100 健康 | 首页点击右上角搜索，核对搜索框连续移动扩展且不被 Header 遮挡、不二次拐动；落地后可输入。网址库右上角无重复入口，Ctrl/Cmd+K 只聚焦目标；再验收铺满偏好与减少动态效果 |
| Q38 Agent 输入焦点视觉 | 内层 textarea outline 与外层 focus-within 已收敛为单层；Hover 中性、聚焦使用令牌混合弱边线/8% 光晕，追问框与 forced-colors 同步；前端 216/216、TypeScript、全前端 ESLint、Next 构建与 diff 通过 | 首页输入框在未聚焦、Hover、鼠标/Tab 聚焦下不得再出现内层亮绿矩形；深浅主题和对话追问框保持一致 |
| Q39 Agent 检索范围与联网切换 | 后端 collection/online URL 来源门禁、真实搜索 URL 登记、无命中切换信号与 scope prompt 测试通过；前端 online-offer 契约、按钮渲染、同会话重试静态检查、TypeScript、定向 ESLint 与 diff 通过；未调用真实 Provider | 仅网址库询问库内无匹配主题时只能说明未找到并出现“开启联网搜索”；点击后顶部切为允许联网、同一会话追加原问题并出现真实 Provider 阶段；联网模式先查库，结果不足才展示真实搜索结果；未配置搜索 Provider 时不能伪造外部网址 |
| Q40 Agent 网址库完整结果与分页 | `search_library` 默认 `include_all=true`，完整结果按查询词确定性排序并持久交给 artifact；模型误传预览时服务端恢复最近完整集合；105 条全量、默认参数、预览恢复后端测试通过，前端现有 12 条/页分页测试通过；未调用真实 Provider | 询问“把库里的所有 AI 类网站都发给我”，核对标题总数与实际卡片一致；105 条应为 9 页（前 8 页各 12 条、末页 9 条），22 条应为 2 页；数字页码、上一页/下一页、刷新/历史恢复均不能丢失或重复；精选/指定数量请求仍只显示指定条数 |
| Q41 Agent 来源信任、URL 安全与容量硬化 | manifest v3 + v2 只读兼容、v3 同消息 `source-url` 外链白名单、旧模型知识/旧搜索重放再校验、Provider URL 工具/runner 双层 canonicalize/filter/dedupe、非规范数字 IPv4/组播地址/畸形 IPv6/单标签与 `home.arpa` 等私有后缀/反斜杠/编码 authority 拒绝、流式文本 UTF-8 安全投影与 JSON fail-closed、256/384/64 KiB 预算、显式截断摘要、可执行草稿优先级、同 query 站内优先联网门禁、失败后联网重试恢复和显式 N 条服务端冻结均有回归；前端 222/222、后端 708/708、完整 lint/typecheck、Next.js 静态页 12/12 与根级 `pnpm check` 通过 | 默认仅网址库不能出现可点击库外地址；显式联网必须用同 query 先查库。以受控假 Provider 混入私网、凭据、敏感参数、数字 IP、反斜杠和公网 URL，确认只有公网项进入实时来源/历史/重放。请求“精选 15 个”时命中足够应恰为 15 条并可刷新恢复；大于 256 KiB 的清单应明确要求缩小范围，不能展示截断清单 |

### 真实 Chrome 手动验收清单

> Q16 起，打开首页或资料库会自动、低并发地开始扫尾历史 `not_analyzed` 网站。Q30 的首页工具栏
> 是另一条显式、可恢复的有界任务：快速模式不调用 LLM，完整模式每批最多 100；重复点击会加入同一任务，
> 不会叠加抓取。预览图只取网站公开
> 声明的 `og:image` / `twitter:image`；完整读取页面但没有公开图会记为“已检查”，不会在下一轮重复下载；
> 超时、失败或截断页面仍可重试。没有公开图不算失败，也不会为了填满而截图。

1. 新建一个正常网站和一个已知会返回 403/反爬页面的网站，等待各自后台分析结束；确认可用的
   站点根图标出现，确实没有图标时才显示本地默认图标，页面没有向第三方 favicon CDN 请求。
2. 打开带 `preview_url` 的网站详情：预览位于紧凑信息行；点击后原图放大，关闭按钮、背景点击
   和 Escape 都能退出，关闭后焦点回到预览按钮；坏图不会留下空占位。
3. 在资料库网格和列表各进入一次批量选择：checkbox 不打开详情、不启动拖拽；分别核对“全选”
   等于当前筛选快照、“全选（已加载）”等于当前实际加载数量且可超过 100。先取消一次确认数据
   不变，再用 2 个临时网站完成一次确认删除；大批量场景核对进度按每批最多 100 条递增。
4. 保持已有筛选向下滚动，分别观察置顶区与普通区在接近末尾前静默接续；切换分类/标签后不出现
   上一个结果集的卡片，也没有重复项或手动“加载更多”按钮。
5. 继续完成 Q14 检查：Provider 页不混入书签/索引内容；已收录卡片先进入站内详情；真实模型
   回答流式渲染，reasoning 可折叠且耗时/Token 只显示 Provider 实际返回值。
6. 打开首页或资料库一次，等待一小段时间：历史网站应以低并发逐步进入 `pending` 后完成；滚动到
   含待分析网站的分类时只刷新附近分区。书签导入后，最新少量网址应优先开始补全，其余继续后台扫尾。
7. 配置真实模型 Provider 后启动首页全库补全，抽查分类、标签、简介和详细介绍；先手工修改其中一个网站的
   四类字段再继续任务，确认人工值不被覆盖。临时停用 Provider 或使用无效模型时，任务应停止扩散请求，
   而不是把几千个网站逐个打到同一错误端点。
8. 在 Agent 输入 `http://randomiban.com/入库` 和 `/存入 https://example.com`，两者都应直接出现
   待确认草稿；生成草稿阶段不消耗模型，确认后网站才真正写入，随后的自动资料分析可能使用 Provider。
   首页应立即出现新网站，详情来源应为“由 Agent 收录”；刷新当前会话后确认卡仍是已保存。打开任一
   网站详情点击“AI 分析入库”，核对提示与实际分类、标签、简介、详细介绍一致，失败不能再显示为绿色成功。
9. 在搜索服务设置中添加 Tavily、Jina 或 Exa，确认按钮明确写着“测试搜索（可能消耗额度）”；主动点击
   后应得到成功、鉴权失败、额度不足、限流或上游失败之一的可操作结果，且界面和日志不泄露密钥或厂商原文。
10. 添加并启用“Exa MCP 免费额度”：表单不要求 Base URL 或 API Key，并显示共享额度、低频范围和
    搜索词会发送给 Exa 的提示。Agent 开启“允许联网”后发起一次需要实时网页资料的问题，搜索卡片应
    显示真实 Provider 名称；未启用联网或未添加搜索 Provider 时不得偷偷发送请求。
11. 选一个正文正常站点和一个超时、403 或正文极少的国外站点，分别点击详情页“AI 分析入库”：前者
    应优先使用原页，不额外搜索；后者可用最多 3 条同域搜索摘要补齐分类、标签、简介和详细介绍。搜索摘要不得填充
    favicon/preview。仅启用免费 Provider 时启动全库批量补全，系统不得调用共享免费额度；原页证据不足的
    当前网站应记为受限，整批继续处理其他网站。换成 Tavily、Jina 或 Exa 后才允许批量搜索补证。
12. 打开任一网站的编辑弹窗，检查视口居中、背景遮罩、关闭与 Escape 的退出动画，并在保存请求期间
    确认所有退出方式都被锁定；在宽屏网址库切换“居中阅读/铺满页面”并刷新，确认偏好保留且刷新时不先
    播放一次自动伸缩。随后分别用“仅网址库”和“允许联网”询问多个网站推荐，
    观察所有工具行动持续显示、思考区可自由折叠且不出现 React 错误；最终具体网站不应再以 Markdown
    表格或纯文字列表重复，已收录项进入站内详情，未收录项保留“收录/打开”。
13. 在新增或编辑网站弹窗输入一个从未存在的标签，点击“新建标签”：标签应立即出现在账号标签库并
    自动勾选。再输入大小写、全角或空白形式不同但规范化相同的名称，应复用原标签而不是创建重复项。
    随后让 Agent 分析相近主题网站，检查它能否优先复用已有近义标签；若仍出现语义重复，记录成对样本，
    不要直接以未经校准的 embedding 阈值自动合并。
14. 新增或重新分析 `youtube.com/watch?v=...`、`youtu.be/...`、`shorts`、`embed` 或 `live` 视频链接，
    在目标页无法直连时仍应显示 YouTube 官方 favicon；合法 11 位视频 ID 应显示对应视频封面。普通站点
    不得套用该回退，页面自己声明的 favicon/`og:image` 应优先于平台默认值。
15. 在宽屏网址库切换“居中/铺满”，确认只有当前项使用产品绿色文字、浅绿色背景与边框高亮。
16. 使用会产生较长回答的真实模型观察 Agent：文本和 reasoning 应随 SSE 事件逐块出现，而不是等回答结束
    后整段弹出。该项必须通过 Next 同源入口检查，不能用后端直连结果代替页面验收。
17. 新增一个需要较长抓取或搜索补证的网站并停留详情页；即使超过旧的 32 秒窗口，也应在五分钟级有界
    刷新内自动从“正在分析”切到真实终态。切换标签页再返回应立即补读一次，终态后不应继续轮询。
18. 让 Agent 创建一个空 Space，再分别尝试“创建论坛 Space 并加入 5 个网站”和“把这些网站加入已有
    Space”：每轮应只有一张任务卡和一个确认按钮。先剔除一个候选、恢复后再剔除另一个并确认，成员结果
    必须等于最终选择；另起一轮用文字说“不要 Bitcoin Forum”，旧卡应失效，新卡保留其余完整清单。
    对同一卡快速双击，或在请求超时后重试，不能重复成员或创建第二个 Space；刷新页面或从另一标签页
    重放也必须得到同一结果。若 Agent 回答未完整落库，卡片应永久禁用并提示重新生成。
    在 Space 页打开新建、重命名、删除和移出弹窗，确认它们位于视口中心，进退动画和保存中关闭锁正常。
19. 在 Chrome 或 Edge 侧载 `apps/browser-extension` 后刷新 WebHub，打开含 3 个网站的“论坛网站” Space；
    状态应显示助手已连接，一次生成 3 个标签和名称为“论坛网站”的绿色展开组。停用扩展并刷新后应明确
    显示未连接，只保留可能被浏览器拦截的普通标签降级；成员超过 100 个时不得创建任何标签。
20. 发起较长 Agent 回答后上滚阅读，确认不被新 chunk 拉回；停止后刷新仍能看到部分正文、思考、来源和
    “已停止”状态，草稿不可执行。对同一回合模拟响应丢失/并发重试，不得重复调用 Provider 或增加消息；
    Tavily、Jina、Exa 联网回答应出现折叠真实来源。检查工具步骤收口、真实 Token、复制操作和共享加载态。
21. 首页先将“快速补全”上限设为较小值，确认计划明确“不调用 LLM”且只补网页元数据；再切换
    “LLM 完整分析”执行小批次，确认预估、固定任务总数、刷新恢复和终态计数一致。Provider 限流时，
    已处理结果保留、未领取项显示为跳过，并给出不含厂商原文的安全提示。
22. 打开网址库点击“排查相似网站”，确认扫描前明确说明本地规则、默认全保留；检查三个 Tab、总页数、
    数字页码/前后翻页、始终可见的底部执行栏和手机宽度。同组连续选择两张以上卡片，确认各卡独立取反、
    未选项显示“将清理”，右上角切为“保留所选”；取消到空集后恢复“全部保留”。分别在每个 Tab 点
    “一键选择推荐”，确认只影响当前分区。刷新后多选决定与总数应恢复。先进入二次确认再返回，确认数据
    不变；最终只对专门准备的少量测试组执行，核对未选成员清理、多个选中成员保留、关系合并与并发保护。
23. 打开 `www.smzdm.com` 详情并执行“AI 分析入库”，确认不再显示“本机、内网”安全错误，图标与分析
    状态能刷新；当前 WAF 页缺少可读正文时允许简介仍受限。随后让 Agent 推荐 NGA、Stage1、酷安，再说
    “把刚才推荐的三个收藏”，应直接生成含三个精确 URL 的单张确认草稿，不得索要网址或提前声称入库。
24. 在 `/settings/import` 从 Windows 资源管理器把一个浏览器导出的 HTML 分别拖到 Header、页面正文与
    窗口边缘；提示应覆盖整个窗口且不闪烁，松手后进入与“选择文件”相同的解析流程。再拖非 HTML 和两个
    HTML，确认只提示格式错误/一次一个文件，当前预览不被清空，本地文件不会被浏览器直接打开。
25. 在书签相似组第二页完成一组决策，确认数字页码和总页数保持且不会跳回第一页。发起带 reasoning 的
    Agent 长回答，确认流开始自动展开、完成自动收起；自动收起后手动开合，不会被普通渲染覆盖。最后在详情页执行真实
    “AI 分析入库”，核对抓取、证据、等待/调用模型、保存等提示来自真实后端阶段，终态后停止轮询并收口。
26. 依次打开首页分类排序、网址库标签与排序、网站新增/编辑分类、Provider 模型目录和 Space 排序；
    深浅主题下展开层均应使用站内颜色、圆角、阴影、绿色焦点与 Check 图标，不得再出现 Windows 原生蓝色菜单。
    用方向键/Home/End/Enter/Escape/Tab 验证选择与焦点；网址库无搜索词时“相关度”不可选，Provider 长模型
    列表可搜索并可切回手填。网站和 Provider 弹框中第一次 Escape 只关下拉，第二次才关弹框；再检查窄屏边缘
    不越界、选项可滚动。Agent 历史菜单与账号菜单也应支持 Escape 并恢复触发器焦点。
27. 在首页点击右上角搜索入口，确认搜索框连续移动并扩展到网址库工具栏，不被吸顶 Header 遮挡、不中途消失、
    不在铺满模式下二次拐动；落地后光标可立即输入。网址库右上角不应再有重复入口，Ctrl/Cmd+K 只聚焦工具栏。
    再开启系统减少动态效果，确认改为直接跳转聚焦；快速连续点击不能重复导航或留下转场状态。
28. 刷新首页后依次观察 Agent 输入框的未聚焦、Hover、鼠标聚焦和 Tab 键聚焦状态；只能显示一层柔和外框，
    textarea 内部不得再出现亮绿色矩形。切换深浅主题并进入一次对话，确认追问输入框使用相同的焦点层级；
    Windows 高对比模式下仍应保留清晰可见的系统焦点轮廓。
29. 默认保持“仅网址库”，先验证模型正文、reasoning 和旧模型知识卡不能把未受信库外地址变成可点击链接；
    显式切到“允许联网”后，以完全相同 query 先查库再搜索。用受控假 Provider 同时返回正常公网 URL 与
    私网、凭据、敏感 query/hash-router 参数、非规范数字 IPv4、编码 authority、反斜杠 URL，确认只有
    正常公网项进入实时来源、刷新历史和同 turn 重放。再请求“精选 15 个 AI 网站”，命中足够时结果必须
    恰为 15 条；超过 256 KiB 的清单必须显示 `result_set_too_large`，不能把截断内容称为完整结果。

## 不能跑的

| 缺口 | 说明 |
| --- | --- |
| Observer 未在真实浏览器验证 | Q6 拖拽持久化、Q9 的 `data-stuck`·`data-compact` 与 Q15 资料库静默分页触发时机，见下方「环境陷阱」 |
| Q14-Q41 页面功能未全部手动验收 | Q19 已完成 `0011` 迁移、API/前端启动和一次单站浏览器复现；Q21 已完成 `0012`、Q24 已完成 `0013`、Q26 已完成 `0014` 迁移。Q27 已用当前真实 Provider 验证模型目录与最小对话恢复。Q28-Q31 的完整结果、两处相似决策与真实清理页面由用户继续检查上表与真实 Chrome 清单；Q32 已真实验证 `www.smzdm.com` 非 LLM 抓取，Q34 已隔离验证浏览器事件和视觉；Q35 的保持当前页、思考自动开合与真实 LLM 阶段、Q36 的主题控件、Q37 的共享搜索转场、Q38 的 Agent 输入焦点视觉、Q39 的仅网址库/联网切换、Q40 的完整结果分页，以及 Q41 的来源/URL/容量边界仍由用户验收。开发库已到 `0019`。 |
| 免费搜索的可用性与批量能力 | “Exa MCP 免费额度”依赖 Exa 官方共享额度，不承诺可用性，只支持低频 Agent/单站分析。公共部署、全库批量和稳定生产流量仍需用户配置 Tavily、Jina、Exa 等自有搜索 Provider；当前没有自建 SearXNG 适配器 |
| 新站自动分析意图尚未完全持久化 | 网站写库本身不会丢；若 256 条账号队列与单个交互溢出槽同时占满，或 API 进程在执行前崩溃，数据库驱动扫尾仍可补网页元数据，但本次即时 LLM enrichment 意图没有持久任务保证。网站可进入下一次首页显式补全；若要保证“本次必做 LLM”，需把新站意图并入持久 run，而不是扩大内存队列。 |
| 近义标签没有确定性语义合并 | 同名身份已用 NFKC、空白折叠和大小写规则统一，Agent 也会读取并优先复用已有近义标签；但“教程/教学”等语义关系仍依赖模型判断。应先积累用户确认的同义/非同义样本，再评估每账号 embedding top-k、阈值与必要的 LLM rerank，避免误合并不同含义。 |
| 删除与 Space 新增成员的极窄并发窗口 | 单删与批删都先快照关联 Space、再删 Site；若另一事务恰在两步之间新增成员，外键级联可能删除新成员但漏涨该 Space 版本。常规账号隔离/乐观锁/整批回滚已覆盖；后续需先取得 Site 写锁并补并发测试 |
| 自定义兼容 Provider 无 Token 用量 | `openai_compatible` 是否接受 `stream_options.include_usage` 无法自动判断；当前只对内置 OpenAI / DeepSeek 开启，需增加显式能力开关 |
| 远程 favicon / preview 未代理缓存 | 出站抓取已安全校验，但页面仍直接加载远程图片；完整修复需要账号鉴权的图片代理、DNS/重定向复验、大小/MIME 校验、缓存与 SVG 策略 |
| 分类图标不能手选 | 只有后端按名称推断，没有前端选择器；契约层 `icon` 已就绪，缺的是一块 UI |
| Agent 聊天里确认导入 | `propose_bookmark_import` 后端出草稿，但前端无契约/无确认通道；只能去 `/settings/import` 手动 apply |
| 书签来源事实未持久化 | PRD 要求长期保留源目录与 occurrence，但 `bookmark_source_occurrences` / `site_import_origins` 当前无人写；暂存预览能用，入库后无法回答“哪条源书签生成了哪个网站” |
| 十万条完整导入尚未可靠 | Q33 已证明 100k 可在约 77-79 秒到达解析预览，但 RSS 峰值约 182-400 MiB、SQLite 增量约 347-393 MiB；深层祖先链有 O(k²) 退化，finalize/apply 仍有全量 ORM、长写锁和并发 `database is locked` 风险，完成任务无 GC。取消、重启恢复、最终 apply 与大库完整相关度/向量检索仍未过门禁 |
| 数据导出与自有备份导入未实现 | PRD P0 仍包含数据导出和自有备份导入；当前只有 `source="backup"` 数据模型预留，没有导出/恢复 API、任务或页面 |


---

## 刻意没做的（**别当成 bug 去"修"**）

这几处是权衡后的决定，不是遗漏。要改请先读理由：

1. **暂不部分填 `bookmark_source_occurrences` / `site_import_origins`。**
   这两张表在 schema 里但全仓库无人写，「哪条书签变成哪个网站」的溯源链整体未建。
   半填一份只会造出一张看起来权威、实际只覆盖走过某个函数的导入的表；这不是取消 PRD，
   完整来源台账仍作为 P0 缺口列在「不能跑」与「下一步」。
2. **书签导入的幂等性不靠来源台账。** Q29 起先用 job 状态/版本 CAS 阻止完成任务再次 apply，
   再由 `sites` 的 `UNIQUE(user_id, identity_url)` 防住并发库写；不要另加会与真实状态漂移的“已导入”表。
3. **解析 worker 是进程内的**，不是 schema 预留的租约 + 心跳式。
   单用户本地应用，为一次 1.7 秒的解析配任务队列是仪式。状态机逐步照走，
   以后换真 worker 不用改 schema。
4. **搜索服务不做无感探针，也不直接执行任意 GitHub Skill。**
   Tavily、Jina、Exa 没有统一只读目录接口，所以连接测试只能由用户显式点击，并明确告知会执行
   一次最小真实搜索、可能消耗额度。“Exa MCP 免费额度”不提供这类测试，批量来源会跳过免费搜索并将
   当前证据不足的网站记为受限，不会停止整批；它使用官方
   MCP SDK 的固定工具适配器。GitHub Skill 只能作为设计参考，未经审计的仓库脚本不能进入运行时。
5. **向量检索只用于网址库页面的「相关度」排序，尚未接入 Agent `search_library`。**
   Q11 已完成 embedding 回填和关键词/语义融合；网址库页面带搜索词并选择「相关度」时才会尝试语义召回，
   没有可用向量时正常降级为关键词。Agent 未指定数量/全量和显式 N 条结果按本轮 query 的确定性关键词
   相关度排序；只有无明确数量、由模型选择 `include_all=false` 的主观有限精选才按 `updated desc`。
   这些 Agent 路径都不消费 Embedding，也没有任意规模结果集 API。
6. **预览截图不做**（需要无头浏览器，成本与安全面不成比例）。
   注意区分：Q13 起 `Site.preview_url` 会存**页面自己声明的** `og:image`
   / `twitter:image`（抓取时顺带拿到，只补空不覆盖）。那不是截图，
   页面没声明就是 None。别因为有了这个字段就去接无头浏览器。
7. **`space-contract` 的 `identifier` 不并入 `contract-guards`。**
   它带长度上限且拒绝数字，共享版为兼容历史整数主键会把数字转成字符串。
   同名不等于同一件事，合并会悄悄放宽 Space 成员 id 的约束。

## 架构约定（2026-07-27 自查后确立）

模块依赖是**无环 DAG**，加新 import 前先确认方向：

```
auth →（无）        底座
bookmarks → auth
library / spaces / chat → auth (+library)
providers → auth
agent → 全部        顶层编排，只有它可以依赖所有模块
```

低层模块需要高层的类型时，**声明 Protocol 描述所需，而不是反向 import**
（见 `bookmarks/classifier.py` 的 `ModelEndpoint`）。

前端契约层的校验原语统一在 `lib/contract-guards.ts`，
各模块传入自己的错误工厂。**合并任何"看起来一样"的函数前必须先证明逐字节相同。**

---

## 环境上必须知道的四件事

1. **Provider 主密钥**：开发环境首次使用自动生成，落在 `.data/provider-master.key`。
   **弄丢它，所有已存的 API Key 永久无法解密。** 生产环境仍要求显式设
   `WEBHUB_PROVIDER_MASTER_KEY`。配置了但格式错误时 fail closed，绝不顺手生成新的。
2. **`.env` 现在真的会被加载**（`config.py` 导入时 `load_dotenv`，`override=False`）。
   在此之前 `.env.example` 里每一项都是哑弹——查配置问题时别再假设它没生效。
3. **内嵌 Browser 面板与用户真实 Chrome 是两套 cookie。**
   「我明明登录了你怎么看不见」十有八九是这个。
4. **内嵌 Browser 面板里 IntersectionObserver 和 CSS transition 都不工作。**
   面板 `visibilityState` 恒为 `hidden`、不合成帧，所以 IO 回调不投递、过渡停在起始值。
   复现：新建一个 IO 观察 `document.body`，1.5s 内零回调；给 `.app-header` 加
   `data-compact` 后 `height` 仍读到 64px，`style.transition='none'` 后立刻变 56px。
   **别把这个当代码 bug 去修。** 需要验证吸顶/懒加载/动效的触发时机，只能：
   手动置位属性验证样式那一半，或者请用户在真实浏览器里跑一遍。
   同理，`api` 挂了时页面停在「正在加载账号...」——先 `curl 127.0.0.1:8100/api/health`。
   端口被上一轮的僵死进程占住时，`Get-NetTCPConnection -LocalPort 8100` 找 pid 再杀。

---

## 下一步

**已登记的 Q1-Q41 当前代码项与 Q41 最终后端/组合门禁已完成，但整个 PRD 尚未完成。以下是尚存的验收与 P0/优化缺口，按影响排序：**

1. **先验收 Q41 来源/URL/容量边界，再验收 Q40-Q35 及 Q32-Q26 既有页面流程**：
   先刷新首页，确认 Agent 输入框在未聚焦、Hover、鼠标/Tab 聚焦时只有一层柔和外框，并核对深浅主题、
   对话追问框与 Windows 高对比模式。先在 Agent 选择“仅网址库”询问库内无匹配主题，确认只能说明未找到
   并出现“开启联网搜索”；点击后确认顶部切为“允许联网”、复用同一会话追加原问题，并在有 Provider 时
   出现真实联网阶段。再用库内有命中和库内无命中的问题核对联网模式始终先查库，未配置 Provider 时不得
   伪造外部网址。随后点击右上角搜索，确认共享搜索框连续移动到网址库、落地聚焦且
   网址库不再显示重复入口；再检查铺满偏好、
   Ctrl/Cmd+K 与减少动态效果。随后依次检查首页、
   网址库、网站弹框、Provider 与 Space 的下拉展开层，确认深浅主题、禁用项、长列表搜索、键盘导航、
   Escape 分层、焦点恢复和窄屏边界均正常，且不再出现 Windows 原生蓝色菜单。随后在书签相似组第二页
   完成一组决策，确认仍停留当前页；发起带 reasoning 的真实回答，确认开始展开、完成收起且手动状态不被
   普通渲染覆盖；在站点详情执行“AI 分析入库”，核对抓取、整理证据、等待/调用模型和保存结果均由后端
   真实阶段驱动，终态后停止。随后在 `www.smzdm.com` 详情确认 Fake-IP 不再误报内网；再让 Agent 推荐
   三个未入库论坛并用“刚才推荐的三个”
   触发同一张精确收录草稿。随后在网址库执行只读扫描，检查高度相似/同站页面分区、分页、默认全保留、组内多选取反与刷新恢复；
   只用少量测试组完成二次确认清理，核对未选成员删除、多个已选成员保留及关系只汇入确定主项。再在
   首页用小上限执行“快速补全”，确认计划
   明确不调用 LLM；用可用 Provider 执行小批“LLM 完整分析”，
   检查预估、刷新恢复、人工字段保护与限流收尾。随后在
   `/settings/import` 导入含同站多页面的真实书签，检查分页、推荐主页、逐组/全部决策、预计新增、未决
   门禁、合并与保留实际结果，以及人工分类、端口和共享平台隔离；再让 Agent 执行“把库里的
   所有 AI 类网站都发给我”，核对实际 matched_count（例如 105 条应为 9 页、22 条应为 2 页）、按查询
   相关度排序、无重复遗漏、刷新恢复和旧版本提示消失；这些数值只是分页样例，不证明任意规模或 100,000
   条 Agent 交付。再请求“精选 15 个 AI 网站”，命中足够时应由 `result_set_id` 恰好交付 15 条；若完整
   artifact 超过 256 KiB，必须返回 `result_set_too_large` 并要求缩小范围。再在可控条件下
   让 Provider 域名解析为
   `198.18.*`，验证新回合明确提示 Fake-IP、设置页测试给出同类诊断且没有外部请求；恢复全局
   `fake-ip-filter` 后新回合成功，旧失败回执刷新后仍保持原错误。`20260729_0014` 已应用；继续验证
   流中上滚、停止后刷新、
   删除会话后同请求重试不重复 Provider/消息、分享链接不携带无关参数、真实来源、四种历史状态、
   未完成草稿禁用、工具步骤与统一加载态。
2. **用户验收 Q25 浏览器原生分组**：在 Chrome/Edge 分别侧载助手，验证连接状态、同名绿色分组、
   响应丢失重试不重复建组、未安装时的准确降级，以及超过 100 个成员时明确拒绝。
3. **用户验收 Q24 Agent Space 闭环**：验证 Agent 创建空 Space、一次确认批量加入、候选剔除/恢复、
   自然语言替换草稿、旧卡冻结，以及双击/超时/跨标签页重放不重复写入；同时检查 Space 新建、重命名、
   删除、移出弹窗的居中与动效。
4. **用户验收 Q23 五条修复**：检查表单创建并立即选中共享标签、Agent 复用近义标签、YouTube 官方
   favicon/视频封面、布局当前项绿色、经 Next 同源入口的逐块 SSE，以及耗时两分钟以上的新站分析能否
   自动从陈旧转圈切换到真实终态。
5. **用户验收 Q22 网址库与 Agent 交互**：检查弹窗居中和进退动画、网址库居中/铺满切换与偏好记忆、
   长 reasoning 流不再出现 React 更新循环，以及站内/联网/模型知识推荐均稳定生成统一网站卡片。
6. **用户验收 Q20-Q21 搜索与双层介绍**：分别配置一个自有搜索 Provider 和“Exa MCP 免费额度”，检查真实
   搜索连接测试、Agent 联网搜索、受限站点单站补证、目标域名/子域过滤、简介与详细介绍长度，以及批量来源跳过免费 Provider、
   当前证据不足的网站记为受限后整批继续。
   真实搜索会向服务商发送查询并可能消耗额度，只由用户主动执行。
7. **用户用真实 Chrome 验收 Q14-Q19 页面能力**：Provider/语义索引分区、真实 favicon、
   紧凑预览放大、站内详情、Agent 流式富文本、100 条批量删除、网址库静默分页、历史元数据
   自动扫尾，以及首页持久化批量补全的进度/重启恢复/同源限流。打开一次首页或网址库后让后台
   低并发运行，核对可见卡片是否自行更新。
8. **补齐书签长期来源事实**：把确认入库的源 snapshot、目录、occurrence 与最终 Site 原子发布到
   `bookmark_source_*` / `site_import_origins`，覆盖重复导入、跳过项与账号隔离；在完整闭环前不写部分台账。
9. **先硬化再完成十万条端到端门禁**：修复深层祖先链 O(k²)，将 finalize 相似投影和 apply 写入改为
   有明确发布边界的批处理，补 SQLite 锁冲突恢复、磁盘余量门禁与终态任务/原始快照 GC；随后验证
   100,000 occurrence 的取消、重启恢复、最终提交、幂等重放，以及 100,000 Site 下不截断的相关度与
   向量检索。不能把 Q33 的 `parse_preview_ready` 基准等同于完整可承载。
10. **实现数据导出与自有备份导入**：定义版本化、账号级、可校验的导出格式和恢复预览，覆盖网站、分类、
   标签、Space、来源事实与必要设置；恢复必须先预览再确认，不能直接覆盖现库。
11. **持久化新站 LLM 分析意图**：把当前进程内交互队列的单站 enrichment 意图并入持久 run，使队列全满
   或 API 进程重启后仍能明确恢复；保留现有单账号并发、同源串行和 Provider 熔断，不以扩大内存队列替代。
12. **校准近义标签语义复用**：先收集用户确认的同义/非同义标签对，再比较 embedding top-k、阈值和
   LLM rerank；没有样本前不自动合并已有标签，避免不可逆地混淆用户 taxonomy。
13. **大批量快照分段化（仅在万级实测出现 SQLite 写锁时）**：先增加不可领取的 `snapshotting` 状态，
   再分段提交 item，最终原子发布为 queued。不能直接把当前 snapshot 事务拆开，否则并发标签页会
   领取不完整集合，进度分母失真。
14. **持久 origin favicon 缓存**：现有进程内同源缓存已防止一轮任务重复拉取；对大量历史 URL，新增
   账号隔离、过期与失败冷却的 origin 缓存后再分发给子页，优先减少重复图标发现，不能通过提高裸并发
   或第三方 CDN 取巧。
15. **受控图片代理与缓存**：favicon / preview 不再由浏览器直连远程地址；服务端代理必须复用
   当前 DNS 固定与重定向校验，并补认证、内容类型/大小、缓存和 SVG 策略。
16. **收紧删除与 Space 成员写入的并发窗口**：删除读取关联成员前先锁所选 Site 行，或用兼容
   SQLite 的条件 no-op UPDATE 取得写锁，并验证并发 add-member 不会产生未 bump 版本的 Space。
17. **自定义 Provider 的流式 usage 能力开关**：用户显式确认兼容端点支持
   `stream_options.include_usage` 后再启用，不能按名称猜测。
18. **Observer 真实浏览器验证**：Q6 拖拽持久化、Q9 的 `data-stuck` / `data-compact`
   吸顶触发和 Q15 网址库自动分页。代码已有回归测试，但内嵌面板里 Observer 回调被挂起
   （见环境陷阱第 4 条），只能在真实 Chrome 里验证触发时机。
19. **分类图标选择器**：后端 `icon` 字段与契约层都已就绪，只缺一块让用户手选的 UI。
20. **Agent 聊天里确认书签导入**：`propose_bookmark_import` 后端已出草稿，
   前端缺契约与确认通道；`prompt.py` 目前向模型承诺了这个能力，属虚假宣传，
   要么补前端要么改提示。
21. **大契约文件拆分**：`lib/agent-contract.ts` 已超 750 行，超出前端 700 行约定。
    它是契约模块不是组件，优先级低于上面几条。
22. **Q26 P2 加分项**：CountUp 节流数字过渡已完成；书签导入/Provider 表单 Stepper 与短时
    AnimatedContent 等 P0/P1 页面验收稳定后再做，步骤和进度必须由真实状态驱动。


---

## 维护约定

每轮迭代提交前，连同队列条目一起更新本文件的：一句话状态、测试基线、
「能跑/不能跑」两张表、下一步。**只写验证过的事实**：
「实测通过」和「代码写完了」必须分开写，后者不算能跑。
